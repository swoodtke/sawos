[package]
name = "sosrt"
version = "0.1.0"

# Arch-free AND role-free: consumed by the kernel build and by every SOS
# process build alike. Primitives only, so it compiles freestanding for any
# target. See src/lib.saw.
