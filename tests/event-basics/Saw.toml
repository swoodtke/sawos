[package]
name = "event-basics"
version = "0.1.0"

# Same reason as sos/root's: this image carries its own `__saw_rt_*` seams, out
# of the `sosrt` module its `sos` dependency pulls in. See that manifest.
runtime = true

# A root server that makes EVENTS AND A WAITER (design 178 M2 unit 3, D5). One
# thread, so nothing here is about scheduling: what it proves is §2.4's
# accumulation (both modes, including that the counting one saturates) and
# §2.2's level-triggered attach — a signal that lands before anyone waits is
# still there when the wait arrives. The parking half is `event-wake`.
[dependencies]
sos = { path = "../../kernel/sysapi" }

[sos]
emit = "sosimg"
priorities = { background = 0, low = 1, normal = 1, high = 1 }

[sos.riscv32-unknown-none-elf]
linker-script = "../../hal/riscv32/user/root.ld"
native = "../../hal/riscv32/user/syscall.c ../../rt/common_c/support.c"

[sos.aarch64-unknown-none-elf]
linker-script = "../../hal/arm64/user/root.ld"
native = "../../hal/arm64/user/syscall.c ../../rt/common_c/support.c"
