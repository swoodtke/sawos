[package]
name = "pipe-oversized"
version = "0.1.0"

# Same reason as root's: this image carries its own `__saw_rt_*` seams, out of
# the `sosrt` module its `sos` dependency pulls in. See that manifest.
runtime = true

# **A BODY BIGGER THAN THE SLOT** (sawos design 13 D-2; spec §2.1). The fixed
# body maximum is published in `sosabi` — a caller cannot declare the buffer
# without it — so a `len` above it is a mistake the process could have checked,
# which design 178's faults ruling puts outside the status enum entirely. The
# check runs BEFORE the copy, so nothing is read and no staging slot is touched.
[dependencies]
sos = { path = "../../kernel/sysapi" }

[sos]
emit = "sosimg"
priorities = { background = 0, low = 1, normal = 1, high = 1 }

[sos.riscv32-unknown-none-elf]
linker-script = "../../hal/riscv32/user/root.ld"
native = "../../hal/riscv32/user/syscall.c ../../rt/common_c/support.c"

[sos.aarch64-unknown-none-elf]
linker-script = "../../hal/arm64/user/root.ld"
native = "../../hal/arm64/user/syscall.c ../../rt/common_c/support.c"
