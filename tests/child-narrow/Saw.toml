[package]
name = "child-narrow"
version = "0.1.0"

# Same reason as sos/root's: this image carries its own `__saw_rt_*` seams, out
# of the `sosrt` module its `sos` dependency pulls in. See that manifest.
runtime = true

# **A CONNECTION END IT MAY HOLD AND MAY NOT POST THROUGH** (sawos design 18, M4
# unit 4; `designs/010` agenda 7b). Its boot set carries the SAME connection end
# twice, through two different keep masks; it posts through the wide one and
# faults on the narrow one, which is attenuate-at-give proved from the receiving
# side.

[dependencies]
sos = { path = "../../kernel/sysapi" }

[sos]
emit = "sosimg"
priorities = { background = 0, low = 1, normal = 1, high = 1 }

[sos.riscv32-unknown-none-elf]
linker-script = "../../hal/riscv32/user/child.ld"
native = "../../hal/riscv32-common/user/syscall.c ../../rt/common_c/support.c"

[sos.aarch64-unknown-none-elf]
linker-script = "../../hal/arm64/user/user.ld"
native = "../../hal/arm64/user/syscall.c ../../rt/common_c/support.c"
