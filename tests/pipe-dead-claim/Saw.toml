[package]
name = "pipe-dead-claim"
version = "0.1.0"

# Same reason as root's: this image carries its own `__saw_rt_*` seams, out of
# the `sosrt` module its `sos` dependency pulls in. See that manifest.
runtime = true

# **CONSUMED IS CONSUMED** (spec §2.1's single-use; sawos design 14). A resolve
# that delivers a reply destroys the caller's handle entry through the op, so the
# word is stale the instant it returns — and reaching for it again is the ordinary
# `BadHandle` fault design 3's generations exist to draw, not a second answer.
[dependencies]
sos = { path = "../../kernel/sysapi" }

[sos]
emit = "sosimg"
priorities = { background = 0, low = 1, normal = 1, high = 1 }

[sos.riscv32-unknown-none-elf]
linker-script = "../../hal/riscv32/user/root.ld"
native = "../../hal/riscv32-common/user/syscall.c ../../rt/common_c/support.c"

[sos.aarch64-unknown-none-elf]
linker-script = "../../hal/arm64/user/user.ld"
native = "../../hal/arm64/user/syscall.c ../../rt/common_c/support.c"
