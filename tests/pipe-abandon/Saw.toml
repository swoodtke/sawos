[package]
name = "pipe-abandon"
version = "0.1.0"

# Same reason as root's: this image carries its own `__saw_rt_*` seams, out of
# the `sosrt` module its `sos` dependency pulls in. See that manifest.
runtime = true

# **ABANDONMENT IS INFORMATION, NOT AN IMPERATIVE** (spec §2.1, ratified; sawos
# design 14 D-2). Three ways an exchange can end with nobody on the other side,
# and each is a DELIVERED ANSWER rather than a strand: a client that drops its
# claim, a server that drops its obligation, and a connection whose server end
# goes away while a message is still staged.
[dependencies]
sos = { path = "../../kernel/sysapi" }

[sos]
emit = "sosimg"
priorities = { background = 0, low = 1, normal = 1, high = 1 }

[sos.riscv32-unknown-none-elf]
linker-script = "../../hal/riscv32/user/root.ld"
native = "../../hal/riscv32-common/user/syscall.c ../../rt/common_c/support.c"

[sos.aarch64-unknown-none-elf]
linker-script = "../../hal/arm64/user/root.ld"
native = "../../hal/arm64/user/syscall.c ../../rt/common_c/support.c"
