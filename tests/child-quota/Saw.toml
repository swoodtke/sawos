[package]
name = "child-quota"
version = "0.1.0"

# Same reason as sos/root's: this image carries its own `__saw_rt_*` seams, out
# of the `sosrt` module its `sos` dependency pulls in. See that manifest.
runtime = true

# THE CHILD THAT MEETS ITS BUDGET (sawos design 7 D-3). It creates Events until
# the kernel refuses, reports WHICH refusal it met, then releases one and
# creates again — the credit proven from userspace rather than from a kernel
# counter nobody can see. Its limit is `DEFAULT_QUOTA_EVENTS` and the Event slab
# is twice that, so the machine was demonstrably not what stopped it.
[dependencies]
sos = { path = "../../kernel/sysapi" }

[sos]
emit = "sosimg"
priorities = { background = 0, low = 1, normal = 1, high = 1 }

[sos.riscv32-unknown-none-elf]
linker-script = "../../hal/riscv32/user/child.ld"
native = "../../hal/riscv32/user/syscall.c ../../rt/common_c/support.c"

[sos.aarch64-unknown-none-elf]
linker-script = "../../hal/arm64/user/child.ld"
native = "../../hal/arm64/user/syscall.c ../../rt/common_c/support.c"
