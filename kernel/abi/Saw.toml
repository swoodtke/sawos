[package]
name = "sosabi"
version = "0.1.0"

# KERNEL-INTERNAL. Not userspace API: this package exists so the kernel's
# dispatch tables and the `sos` wrapper module it exports can share one set of
# numbers, and for no other reason. Userspace links `sos` and never names a
# number. See src/lib.saw.
