---
{"action":"patch_add","actor":"agent:claude-sawos","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note"},{"checked_at":"","checked_by":"","text":"The Sep 7 bump's content lands unchanged"},{"checked_at":"","checked_by":"","text":"The slab + thread-preempt fixes land"},{"checked_at":"","checked_by":"","text":"`make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[{"author":"agent:claude-sawos","body":"Submitted patch SO-21.p1 (revision 1): Pin sawlang at 454207de (0.11.1): Sep 7 bump + slab borrows -> &var T + thread-preempt ';' split","created":"1790266592","id":"c1"}],"created":"1790266548","description":"## Scope\n\nPin sawlang at current main **`454207de`**, which reports **0.11.1**. **User ruling (Sep 24, in session with claude-sawos): \"pin to head as 0.11.1.\"** No tag is needed. `tools/toolchain.py` never reads tags: the fetch path uses `sha`, and `version` is checked only against a `sawc` on `$PATH`. The version is honest, since head reports 0.11.1. That a real v0.11.1 `sawc` on `$PATH` also satisfies it is D-b2's documented asymmetry for an unreleased compiler, and the pin's comment now says so. That answers claude-sos-reviewer's m158 point (1).\n\nTwo things land together, because GitHub never received the first:\n\n1. **The Sep 7 bump, 0.8.0 → 0.11.1** (`aa9e88f` on the old local main, NEVER PUSHED; chat m167). It is replayed unchanged onto origin/main: its only difference from aa9e88f is ci.yml + .sawtracker. It carries SL-27's `sizeof` derive in pipe.saw, design 245's `chars()` → `scalars()` sweep (`sosrt.ascii_byte` takes a `Scalar`), and the designs/todo.md SL-26/27/28 records. It was gated 382/382 at v0.11.1 on Sep 7.\n2. **What head costs beyond 0.11.1.** A full gate at head found two source breaks and nothing else:\n   - `kernel/core/slab.saw` Slab.[]: `(&var self, i: Int) unsafe borrows -> &var T` (was `&self ... borrows -> T`). SL-333 makes the arrow name the lent reference and its mode, and `-> &var T` requires `&var self`. The body only lends, so the accessor stays SHARED-ELIGIBLE per the spec's \"Receiver access is a second fact\": a read through `EVENTS[i]` still borrows the slab shared. A doc comment says so.\n   - `tests/thread-preempt/src/main.saw:157`: SL-347 refuses two statements on one line with nothing between them. The fix uses the tree's multi-line catch-guard shape (the user's Sep 22 style ruling: one line or fully broken).\n\nA parse check of all 205 tracked `.saw` files with head's parser: clean.\n\n## Not in scope\n\nSL-355's reactor_register ABI (0.12.0) is not at head. SO-18 keeps that (taskdump.saw's ENOSYS port, then the pin to the 0.12.0 cut) and stacks on this.\n\n## Evidence\n\n- Full gate at head (SAWLANG_ROOT = fresh HTTPS clone at 454207de; aa9e88f + the two fixes): **382/382 = 129 + 129 + 124**.\n- Pinned FETCH path (no SAWLANG_ROOT, no sawc on PATH): `toolchain.py` resolves `via: fetch` and cold-clones 454207de with its venv.\n- In progress: the final gate on the exact patch tree via the fetch path, diffed against a v0.11.1 baseline transcript of aa9e88f. Results post here.\n\n## Acceptance\n\n- [ ] `sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\n- [ ] The Sep 7 bump's content lands unchanged\n- [ ] The slab + thread-preempt fixes land\n- [ ] `make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\n","id":"SO-21","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":4,"sequence":43,"stage":"backlog","status":"open","title":"Pin sawlang at head 454207de (reports 0.11.1): land the unpushed Sep 7 bump + head's two source fixes","updated":"1790266592"},"before":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note"},{"checked_at":"","checked_by":"","text":"The Sep 7 bump's content lands unchanged"},{"checked_at":"","checked_by":"","text":"The slab + thread-preempt fixes land"},{"checked_at":"","checked_by":"","text":"`make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790266548","description":"## Scope\n\nPin sawlang at current main **`454207de`**, which reports **0.11.1**. **User ruling (Sep 24, in session with claude-sawos): \"pin to head as 0.11.1.\"** No tag is needed. `tools/toolchain.py` never reads tags: the fetch path uses `sha`, and `version` is checked only against a `sawc` on `$PATH`. The version is honest, since head reports 0.11.1. That a real v0.11.1 `sawc` on `$PATH` also satisfies it is D-b2's documented asymmetry for an unreleased compiler, and the pin's comment now says so. That answers claude-sos-reviewer's m158 point (1).\n\nTwo things land together, because GitHub never received the first:\n\n1. **The Sep 7 bump, 0.8.0 → 0.11.1** (`aa9e88f` on the old local main, NEVER PUSHED; chat m167). It is replayed unchanged onto origin/main: its only difference from aa9e88f is ci.yml + .sawtracker. It carries SL-27's `sizeof` derive in pipe.saw, design 245's `chars()` → `scalars()` sweep (`sosrt.ascii_byte` takes a `Scalar`), and the designs/todo.md SL-26/27/28 records. It was gated 382/382 at v0.11.1 on Sep 7.\n2. **What head costs beyond 0.11.1.** A full gate at head found two source breaks and nothing else:\n   - `kernel/core/slab.saw` Slab.[]: `(&var self, i: Int) unsafe borrows -> &var T` (was `&self ... borrows -> T`). SL-333 makes the arrow name the lent reference and its mode, and `-> &var T` requires `&var self`. The body only lends, so the accessor stays SHARED-ELIGIBLE per the spec's \"Receiver access is a second fact\": a read through `EVENTS[i]` still borrows the slab shared. A doc comment says so.\n   - `tests/thread-preempt/src/main.saw:157`: SL-347 refuses two statements on one line with nothing between them. The fix uses the tree's multi-line catch-guard shape (the user's Sep 22 style ruling: one line or fully broken).\n\nA parse check of all 205 tracked `.saw` files with head's parser: clean.\n\n## Not in scope\n\nSL-355's reactor_register ABI (0.12.0) is not at head. SO-18 keeps that (taskdump.saw's ENOSYS port, then the pin to the 0.12.0 cut) and stacks on this.\n\n## Evidence\n\n- Full gate at head (SAWLANG_ROOT = fresh HTTPS clone at 454207de; aa9e88f + the two fixes): **382/382 = 129 + 129 + 124**.\n- Pinned FETCH path (no SAWLANG_ROOT, no sawc on PATH): `toolchain.py` resolves `via: fetch` and cold-clones 454207de with its venv.\n- In progress: the final gate on the exact patch tree via the fetch path, diffed against a v0.11.1 baseline transcript of aa9e88f. Results post here.\n\n## Acceptance\n\n- [ ] `sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\n- [ ] The Sep 7 bump's content lands unchanged\n- [ ] The slab + thread-preempt fixes land\n- [ ] `make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\n","id":"SO-21","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":3,"sequence":42,"stage":"backlog","status":"open","title":"Pin sawlang at head 454207de (reports 0.11.1): land the unpushed Sep 7 bump + head's two source fixes","updated":"1790266578"},"id":"SO-21","project":"SO","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawos\",\"diff\":\"diff --git a/designs/todo.md b/designs/todo.md\\nindex 5c09c8a..c8a0188 100644\\n--- a/designs/todo.md\\n+++ b/designs/todo.md\\n@@ -410,5 +410,26 @@ One entry per issue, resolution-sufficient: the symptom verbatim, the probe/site\\n   **WHERE IT BIT, and it is the interesting half:** a BUILD PROFILE is exactly a module that re-implements part of another module's seam. SOS's flat profile (design 35) re-exports the riscv32 virt board's device half and answers differently about protection, and `PROT_REPLAY_AT_SWITCH` — a `public static Bool` in the shared arch module since design 27 — could therefore not be overridden by it AT ALL. `prot_reset` and its six siblings ARE declared in both modules and coexist, which is what made the failure read as a spelling problem for an hour. **In-tree resolution: the two seam values a profile must override became FUNCTIONS** (`prot_replay_at_switch()`, `prot_isolated()`), which is livable — a leaf returning a literal still inlines and folds, so `load_domain`'s replay loop still compiles away where it is dead — and which yields a rule worth stating on its own: **a HAL constant that a build profile may need to override cannot be a `static`.** The other HAL constants (`PROT_GRAIN`, `GRANT_ROW_BUDGET`, `FRAME_BYTES`, the memory map) stay `static`s because nothing overrides them; they are declared once and re-exported, which is the shape that works — and which is also why this went unnoticed for eight designs' worth of HAL work.\\n   What it does NOT reach: a `static_assert` operand, an array length, a const generic argument. Those want a compile-time constant, and a function is not one — so a profile that had to override a SIZE rather than a flag would have no spelling at all today, which is the sharper version of this entry and is filed here rather than met.\\n   Resolution shape: give a module-level `static` the identity a free function has (design 249's rule at the fourth declaration kind), so `top.X` and `core.X` are two statics and each importer resolves its own. Failing that, the diagnostic should anchor on the two DECLARATIONS and say which modules they are in, rather than on an entry file that names neither.\\n-- SL-26 — SAW CANNOT ASK FOR ALIGNMENT ON A BYTE BUFFER: a `[UInt8; N]` local has an alignment of ONE, no alignment attribute or aligned-array spelling exists, and the alignment a stack local actually gets is the optimizer's business — so an API whose ABI needs word-aligned bytes (`&[UInt8; N]` into a kernel copy door) cannot state that requirement anywhere in the type system, and every caller compiles into a latent fault (sawc 0.8.0 @ `449d2485`, found at the seventh pin bump, Sep 5). WHERE IT BIT: the whole pipe sysapi surface takes `body: &[UInt8; PIPE_BODY_BYTES]` and the kernel's copy-in door faults an unaligned source; every one of ~20 test programs' `var body: [UInt8; 128]` locals happened to land word-aligned at -O1 and below, and `-Oz`'s frame packing landed TWO of them odd on riscv32 (`pipe_donate`, both riscv32 profiles; arm64 by luck) — the first optimization-level change ever taken, and the gate caught it same-day. Neither a bug in -Oz nor in the kernel: the contract was unstatable, so it was never stated. IN-TREE RESOLUTION (the vDSO discipline): `kernel/sysapi/src/pipe.saw` grew `body_addr_for_kernel` — the four send-side funnels stage the body through a word-typed `[UInt; PIPE_STAGE_WORDS]` frame-local (word-aligned BY TYPE) when and only when the caller's storage is unaligned; aligned callers pay one branch, unaligned ones a 128-byte copy. The receive side was always safe (bodies ride inside word-aligned structs). Resolution shape upstream: an alignment request the type system carries — an `@align(N)` on locals/statics, or an aligned-array type — so a byte buffer with an ABI can say so; the staging copy then becomes deletable. Probe: `tests/pipe-donate` at `-Oz` on riscv32, before the sysapi fix.\\n-- SL-27 — `sizeof`/`alignof` DO NOT FOLD IN A STATIC INITIALIZER, AND THE REFUSAL'S OWN HINT SAYS THEY DO: `static C: Int = sizeof<UInt64>()` is ``error: static `C` must be initialized by a compile-time constant`` whose hint reads \\\"a static initializer is a CONSTANT EXPRESSION ...: literals, arithmetic and bitwise over them, `sizeof`/`alignof`, the integer limits, ...\\\" (sawc 0.8.0 @ `449d2485`, hosted and freestanding alike, probed Sep 5 at the seventh pin bump). Minimal repro is that one line; mixing them into arithmetic (`16 / sizeof<UInt>()`) refuses identically. `sizeof` in a `static_assert`, an array length or a function body folds fine — the gap is the static-initializer position only. WHERE IT BIT: the SL-26 staging buffer wanted `static PIPE_STAGE_WORDS: Int = PIPE_BODY_BYTES / sizeof<UInt>()` (the natural derive-don't-restate spelling of design 186); the workaround in-tree is a literal count over `UInt64` plus a `static_assert` pinning it to `PIPE_BODY_BYTES`. Resolution shape: either fold the two builtins in that position (they are the hint's own promise, and design 186 tier 2 names them) or correct the hint — a hint that lists the refused spelling as allowed costs a round-trip per encounter.\\n+- SL-26 — SAW CANNOT ASK FOR ALIGNMENT ON A BYTE BUFFER: a `[UInt8; N]` local has an alignment of ONE, no alignment attribute or aligned-array spelling exists, and the alignment a stack local actually gets is the optimizer's business — so an API whose ABI needs word-aligned bytes (`&[UInt8; N]` into a kernel copy door) cannot state that requirement anywhere in the type system, and every caller compiles into a latent fault (sawc 0.8.0 @ `449d2485`, found at the seventh pin bump, Sep 5). WHERE IT BIT: the whole pipe sysapi surface takes `body: &[UInt8; PIPE_BODY_BYTES]` and the kernel's copy-in door faults an unaligned source; every one of ~20 test programs' `var body: [UInt8; 128]` locals happened to land word-aligned at -O1 and below, and `-Oz`'s frame packing landed TWO of them odd on riscv32 (`pipe_donate`, both riscv32 profiles; arm64 by luck) — the first optimization-level change ever taken, and the gate caught it same-day. Neither a bug in -Oz nor in the kernel: the contract was unstatable, so it was never stated. IN-TREE RESOLUTION (the vDSO discipline): `kernel/sysapi/src/pipe.saw` grew `body_addr_for_kernel` — the four send-side funnels stage the body through a word-typed `[UInt; PIPE_STAGE_WORDS]` frame-local (word-aligned BY TYPE) when and only when the caller's storage is unaligned; aligned callers pay one branch, unaligned ones a 128-byte copy. The receive side was always safe (bodies ride inside word-aligned structs). Resolution shape upstream: an alignment request the type system carries — an `@align(N)` on locals/statics, or an aligned-array type — so a byte buffer with an ABI can say so; the staging copy then becomes deletable. Probe: `tests/pipe-donate` at `-Oz` on riscv32, before the sysapi fix. **PARTLY ANSWERED (eighth pin bump, sawlang 0.10.0 @ `fd0b5aa8`, Sep 7), and the remainder is named.** DF-300b's `@align(N)` v1 delivers the alignment REQUEST on locals and statics (probe-verified: an `@align(8)` local and an `@align(64)` static both land aligned), so a caller can now state its own buffer's alignment. WHAT IT DOES NOT REACH: the PARAMETER position — the type-carried aligned form is explicitly its own later upstream brief — so `post`/`send`'s `&[UInt8; PIPE_BODY_BYTES]` still cannot DEMAND alignment from callers it never sees, and `body_addr_for_kernel`'s staging funnel stays as the contract's enforcement (its site comment now says why). The staging copy becomes deletable when the attribute reaches a parameter or an aligned-array type exists; the entry stays open on that remainder.\\n+- SL-27 — `sizeof`/`alignof` DO NOT FOLD IN A STATIC INITIALIZER, AND THE REFUSAL'S OWN HINT SAYS THEY DO: `static C: Int = sizeof<UInt64>()` is ``error: static `C` must be initialized by a compile-time constant`` whose hint reads \\\"a static initializer is a CONSTANT EXPRESSION ...: literals, arithmetic and bitwise over them, `sizeof`/`alignof`, the integer limits, ...\\\" (sawc 0.8.0 @ `449d2485`, hosted and freestanding alike, probed Sep 5 at the seventh pin bump). Minimal repro is that one line; mixing them into arithmetic (`16 / sizeof<UInt>()`) refuses identically. `sizeof` in a `static_assert`, an array length or a function body folds fine — the gap is the static-initializer position only. WHERE IT BIT: the SL-26 staging buffer wanted `static PIPE_STAGE_WORDS: Int = PIPE_BODY_BYTES / sizeof<UInt>()` (the natural derive-don't-restate spelling of design 186); the workaround in-tree is a literal count over `UInt64` plus a `static_assert` pinning it to `PIPE_BODY_BYTES`. Resolution shape: either fold the two builtins in that position (they are the hint's own promise, and design 186 tier 2 names them) or correct the hint — a hint that lists the refused spelling as allowed costs a round-trip per encounter. **CLOSED (eighth pin bump, sawlang 0.10.0 @ `fd0b5aa8`, Sep 7):** the DF-300c/DF-294c const-eval leaf batch (one leaf table, one evaluator — DF-307a) folds `sizeof`/`alignof` at the static-initializer position, the minimal repro and the derived `PIPE_BODY_BYTES / sizeof<UInt>()` spelling both verified by probe under sawc 0.10.0. The workaround is swept: `kernel/sysapi/src/pipe.saw`'s `PIPE_STAGE_DWORDS` now DERIVES (`PIPE_BODY_BYTES / sizeof<UInt64>()`); the `static_assert` beside it stays, reworded as the divisibility guard it still is.\\n+- SL-28 — AN `@export` IN THE ENTRY MODULE IS REPORTED AS A DUPLICATE OF ITSELF WHENEVER THE UNIT SPAWNS A SUSPENDING TASK: ``error: duplicate `@export` symbol `my_c_hook` (already exported by `my_c_hook` at line 4)`` — the \\\"duplicate\\\" and the \\\"already exported\\\" are ONE declaration, and the hint (\\\"give one an explicit `@export(\\\"other_name\\\")`\\\") cannot be followed because there is no other one (sawc 0.10.0 @ `fd0b5aa8`, found at the eighth pin bump, Sep 7; the bump is BLOCKED on this). Hosted and freestanding alike, default flags; every `@export` in the entry module fires at once, renamed exports and same-name exports both. Minimal repro, one file, hosted:\\n+  ```\\n+  import std.task.*\\n+\\n+  @export(\\\"my_c_hook\\\")\\n+  func my_c_hook() -> Int {\\n+      0\\n+  }\\n+\\n+  func work() -> Int {\\n+      yield_now()\\n+      1\\n+  }\\n+\\n+  func main() -> Int {\\n+      var group = TaskGroup()\\n+      let h = group.spawn(work())\\n+      h.join() + my_c_hook()\\n+  }\\n+  ```\\n+  Drop the spawn (or the `yield_now`, leaving nothing to transform) and it compiles; move the `@export` into an IMPORTED module and it also compiles — the trigger is exactly {an `@export` in the entry module} × {the coroutine transform running}. BISECTED (git bisect run over v0.9.0..v0.10.0, the repro as the oracle): first bad commit is `39b3d6a1` — design 266 U1, \\\"the post-transform re-entry is DELETED — declarations are ADMITTED\\\". Mechanism, from that commit's own message: `admit_declarations` step 2 is \\\"re-check the entry module with the SAME `TypeChecker`\\\", and the re-check re-registers the entry module's `@export`s into a duplicate-symbol check that is not idempotent — so every entry-module export collides with its own first registration. WHERE IT BIT: `tests/taskdump.saw` (design 158 unit 3), the one kernel entry that both `@export`s its stub seams + `kmain` AND spawns Saw tasks; it fails to BUILD on all three gate profiles (379/382). No in-tree workaround was taken: the entry module needs `@export(\\\"kmain\\\")` for the boot path and the test's whole point is spawning, so dodging would mean moving the test body into a module behind a new per-case runner knob and re-pointing every `taskdump.saw:NN` expectation — surgery worth more than the wait, since everything else under 0.10.0 is green. Resolution shape: make the export registration idempotent under the admit re-check (register keyed by declaration identity, or skip already-admitted declarations at step 2) — the duplicate check itself is right and should keep firing on two DISTINCT declarations. **CLOSED (eighth pin bump landed, sawlang 0.11.1 @ `6dce5cf1`, Sep 7):** upstream DF-300d makes the admission re-check idempotent — a declaration re-presented by the re-check is no longer a duplicate of itself (the v0.11.1 tag commit names this entry) — and the fix was cut as 0.11.1 rather than the anticipated 0.10.1. The minimal repro compiles and runs by probe under sawc 0.11.1, and `task_dump` builds and passes on all three gate profiles. The bump therefore also crossed design 245's BREAKING removal of `String.chars()`/`append_scalar` (0.11.0), swept same-day: `sosrt.ascii_byte` takes a `Scalar` and projects `.value()` inside (the negative-value check dropped — the type's invariant excludes it), and the eight `for … in x.chars()` loops became `.scalars()`. Gate at 0.11.1: **382/382** (129+129+124).\\ndiff --git a/hal/arm64/kernel/lib.saw b/hal/arm64/kernel/lib.saw\\nindex 03dcc3f..c7d5b5f 100644\\n--- a/hal/arm64/kernel/lib.saw\\n+++ b/hal/arm64/kernel/lib.saw\\n@@ -883,7 +883,7 @@ public func syscall_return(frame_addr: UInt, status: UInt, value: UInt) unsafe {\\n \\n /// Write an ASCII string to the console, byte by byte.\\n func report_str(msg: String) {\\n-    for scalar in msg.chars() {\\n+    for scalar in msg.scalars() {\\n         console_byte(ascii_byte(scalar))\\n     }\\n }\\ndiff --git a/kernel/core/diag.saw b/kernel/core/diag.saw\\nindex 1c30257..4c1b224 100644\\n--- a/kernel/core/diag.saw\\n+++ b/kernel/core/diag.saw\\n@@ -79,7 +79,7 @@ extension Console {\\n     /// path's own console output into the middle of the line being written —\\n     /// into the very transcript the acceptance oracle diffs.\\n     public func write_str(&self, msg: String) {\\n-        for scalar in msg.chars() {\\n+        for scalar in msg.scalars() {\\n             hal.console_byte(ascii_byte(scalar))\\n         }\\n     }\\ndiff --git a/kernel/core/slab.saw b/kernel/core/slab.saw\\nindex 0337646..a233dec 100644\\n--- a/kernel/core/slab.saw\\n+++ b/kernel/core/slab.saw\\n@@ -197,11 +197,16 @@ extension Slab<T, N> {\\n     /// `EVENTS[i].state = Live` both keep the spelling they had when `EVENTS` was\\n     /// a bare array.\\n     ///\\n+    /// `&var self` is the SIGNATURE's price, not a mode change (SL-333): a\\n+    /// `-> &var T` lend must name a receiver it may write through. The body only\\n+    /// lends, so the accessor stays shared-eligible — a read still borrows the\\n+    /// slab SHARED at its use site, exactly as the `&self` spelling did.\\n+    ///\\n     /// Out of range PANICS, in the house wording (DF-249a) and for the reason a\\n     /// direct accessor always does: an index past the capacity is a kernel bug,\\n     /// not a caller's mistake — every index reaching here came from a slab scan\\n     /// or from a handle entry the table validated.\\n-    public(package) func [](&self, i: Int) unsafe borrows -> T {\\n+    public(package) func [](&var self, i: Int) unsafe borrows -> &var T {\\n         if i < 0 || i >= self.total {\\n             panic(\\\"Slab.[]: index out of range: {} (len {})\\\", i, self.total)\\n         }\\ndiff --git a/kernel/sysapi/src/pipe.saw b/kernel/sysapi/src/pipe.saw\\nindex 6197c5c..c034185 100644\\n--- a/kernel/sysapi/src/pipe.saw\\n+++ b/kernel/sysapi/src/pipe.saw\\n@@ -912,13 +912,12 @@ func post_body(handle: PipeInletHandle, body: &[UInt8; PIPE_BODY_BYTES],\\n /// The staging buffer's length, in DOUBLEWORDS — `UInt64` rather than the\\n /// machine word, deliberately: a `[UInt64; N]` local is EIGHT-aligned by the\\n /// natural ABI on both targets, which satisfies any machine-word alignment\\n-/// the copy doors could ask, and its count is one literal instead of a\\n-/// per-target `sizeof` division (which a static initializer refused to fold\\n-/// at sawc 0.8.0 — the platform-typed `sizeof<UInt>()` operand).\\n-static PIPE_STAGE_DWORDS: Int = 16\\n+/// the copy doors could ask, and the count derives from the body size\\n+/// (sawc 0.10.0 folds `sizeof` in a static initializer — SL-27 closed).\\n+static PIPE_STAGE_DWORDS: Int = PIPE_BODY_BYTES / sizeof<UInt64>()\\n \\n static_assert(PIPE_STAGE_DWORDS * 8 == PIPE_BODY_BYTES,\\n-              \\\"the staging buffer must cover exactly one pipe body\\\")\\n+              \\\"a pipe body is a whole number of staging doublewords\\\")\\n \\n /// **THE ADDRESS THE KERNEL MAY READ THE BODY FROM** — the caller's own\\n /// storage where it is already word-aligned, a copy in `staged` where it is\\n@@ -927,8 +926,10 @@ static_assert(PIPE_STAGE_DWORDS * 8 == PIPE_BODY_BYTES,\\n /// The copy-in door faults a source that is not word-aligned (the rule\\n /// `kernel/core/process.saw` states once for every kernel record), and the\\n /// public surface takes `&[UInt8; PIPE_BODY_BYTES]` — a byte array, whose\\n-/// alignment is ONE. Saw has no way to ask for more (no alignment attribute\\n-/// exists), so every caller's `var body: [UInt8; ...]` local is word-aligned\\n+/// alignment is ONE. sawc 0.10.0's `@align(N)` lets a caller align its own\\n+/// LOCAL, but the attribute does not reach a PARAMETER (the type-carried\\n+/// form is deferred upstream), so this signature still cannot demand it and\\n+/// an unannotated caller's `var body: [UInt8; ...]` local is word-aligned\\n /// only by the accident of stack layout — which held at every optimization\\n /// level until `-Oz` packed two riscv32 frames differently and the gate\\n /// caught the fault. The sysapi is the vDSO: the contract is made to hold\\ndiff --git a/kernel/sysapi/src/system.saw b/kernel/sysapi/src/system.saw\\nindex 9ceea0d..3e44397 100644\\n--- a/kernel/sysapi/src/system.saw\\n+++ b/kernel/sysapi/src/system.saw\\n@@ -203,7 +203,7 @@ extension System {\\n     /// Write an ASCII string. Stops at the FIRST failure and reports it — a\\n     /// partial write is surfaced, never swallowed.\\n     public func debug_print(&self, msg: String) -> Result<Void, SosStatus> {\\n-        for scalar in msg.chars() {\\n+        for scalar in msg.scalars() {\\n             try self.debug_print_byte(ascii_byte(scalar))\\n         }\\n         return\\ndiff --git a/rt/common/src/lib.saw b/rt/common/src/lib.saw\\nindex 645878d..2633269 100644\\n--- a/rt/common/src/lib.saw\\n+++ b/rt/common/src/lib.saw\\n@@ -79,17 +79,19 @@ public func hex_digit_of(value: UInt, index: Int) -> UInt8 {\\n \\n /// A Unicode scalar as a console byte.\\n ///\\n-/// `String.chars()` yields scalars and the console surfaces on both sides are\\n-/// byte-oriented, so a non-ASCII scalar has to become SOMETHING. It becomes\\n+/// `String.scalars()` yields `Scalar`s and the console surfaces on both sides\\n+/// are byte-oriented, so a non-ASCII scalar has to become SOMETHING. It becomes\\n /// `'?'` rather than a silent truncation to a plausible-looking wrong\\n /// character — the rule is stated once, here, instead of being re-decided by\\n /// each caller. A byte-oriented console service is userspace work for a later\\n-/// milestone.\\n-public func ascii_byte(scalar: Int) -> UInt8 {\\n-    if scalar < 0 || scalar > ASCII_MAX {\\n+/// milestone. `Scalar`'s own invariant excludes negatives, so ASCII-or-not is\\n+/// the only question left.\\n+public func ascii_byte(scalar: Scalar) -> UInt8 {\\n+    let value = scalar.value()\\n+    if value > ASCII_MAX {\\n         ASCII_SUBSTITUTE\\n     } else {\\n-        scalar as UInt8\\n+        value as UInt8\\n     }\\n }\\n \\ndiff --git a/sawlang.pin b/sawlang.pin\\nindex 946c4df..f143a6f 100644\\n--- a/sawlang.pin\\n+++ b/sawlang.pin\\n@@ -3,5 +3,11 @@\\n # BLADE), a `sawc` on $PATH whose --version matches `version`, then a cached\\n # fetch of `sha`. Bump both lines together, rigorously (D-b2: the check is\\n # version-only, so a version that lies about its sha defeats it).\\n-version = \\\"0.8.0\\\"\\n-sha = \\\"449d2485cbdcbddf618b12bbd677e8fb938647e5\\\"\\n+#\\n+# The sha below is an UNTAGGED sawlang main commit (user ruling, Sep 24 2026).\\n+# It reports 0.11.1 because no version has been cut since, so the version is\\n+# honest, and a real v0.11.1 `sawc` on $PATH also satisfies it. That is D-b2's\\n+# accepted asymmetry for an unreleased compiler. The next cut (0.12.0, SL-355)\\n+# closes it.\\n+version = \\\"0.11.1\\\"\\n+sha = \\\"454207de582bd51983f64ae3c255e565c028ebf0\\\"\\ndiff --git a/tests/child-echo-ns16550/src/main.saw b/tests/child-echo-ns16550/src/main.saw\\nindex f271bd1..ba62467 100644\\n--- a/tests/child-echo-ns16550/src/main.saw\\n+++ b/tests/child-echo-ns16550/src/main.saw\\n@@ -168,7 +168,7 @@ extension Uart {\\n     }\\n \\n     func put_str(&self, msg: String) unsafe {\\n-        for scalar in msg.chars() {\\n+        for scalar in msg.scalars() {\\n             self.put_byte(ascii_byte(scalar))\\n         }\\n     }\\ndiff --git a/tests/child-echo-pl011/src/main.saw b/tests/child-echo-pl011/src/main.saw\\nindex 54830af..ba00310 100644\\n--- a/tests/child-echo-pl011/src/main.saw\\n+++ b/tests/child-echo-pl011/src/main.saw\\n@@ -148,7 +148,7 @@ extension Pl011 {\\n     }\\n \\n     func put_str(&self, msg: String) unsafe {\\n-        for scalar in msg.chars() {\\n+        for scalar in msg.scalars() {\\n             self.put_byte(ascii_byte(scalar))\\n         }\\n     }\\ndiff --git a/tests/thread-preempt/src/main.saw b/tests/thread-preempt/src/main.saw\\nindex c0e3f83..0ef017f 100644\\n--- a/tests/thread-preempt/src/main.saw\\n+++ b/tests/thread-preempt/src/main.saw\\n@@ -154,7 +154,10 @@ func initial(boot_handle: UInt) {\\n     // knowing: an interrupt taken while the kernel is IDLE never reaches `ktrap`\\n     // — the idle path polls the controller rather than taking a vector — so this\\n     // column counts what PREEMPTED this process, which is exactly what it is for.\\n-    let cost = try proc.stats() catch { let _ = proc.exit(0)  return }\\n+    let cost = try proc.stats() catch {\\n+        let _ = proc.exit(0)\\n+        return\\n+    }\\n     print(\\\"SOS preempt: interrupts={}\\\\n\\\", cost.interrupts)\\n     let _ = proc.exit(0)\\n }\\ndiff --git a/tests/uart-echo-ns16550/src/main.saw b/tests/uart-echo-ns16550/src/main.saw\\nindex b8f8118..5466d2f 100644\\n--- a/tests/uart-echo-ns16550/src/main.saw\\n+++ b/tests/uart-echo-ns16550/src/main.saw\\n@@ -168,7 +168,7 @@ extension Uart {\\n     }\\n \\n     func put_str(&self, msg: String) unsafe {\\n-        for scalar in msg.chars() {\\n+        for scalar in msg.scalars() {\\n             self.put_byte(ascii_byte(scalar))\\n         }\\n     }\\ndiff --git a/tests/uart-echo-pl011/src/main.saw b/tests/uart-echo-pl011/src/main.saw\\nindex e96e544..8def827 100644\\n--- a/tests/uart-echo-pl011/src/main.saw\\n+++ b/tests/uart-echo-pl011/src/main.saw\\n@@ -166,7 +166,7 @@ extension Pl011 {\\n     }\\n \\n     func put_str(&self, msg: String) unsafe {\\n-        for scalar in msg.chars() {\\n+        for scalar in msg.scalars() {\\n             self.put_byte(ascii_byte(scalar))\\n         }\\n     }\\ndiff --git a/tests/uartproto/src/lib.saw b/tests/uartproto/src/lib.saw\\nindex 22ba263..e3c4527 100644\\n--- a/tests/uartproto/src/lib.saw\\n+++ b/tests/uartproto/src/lib.saw\\n@@ -146,7 +146,7 @@ extension UartBody {\\n     public func text(&var self, op: UartOp, s: String) -> UInt {\\n         self.bytes[OP_BYTE] = op as UInt8\\n         var n: Int = 0\\n-        for scalar in s.chars() {\\n+        for scalar in s.scalars() {\\n             if n < MAX_PAYLOAD {\\n                 self.bytes[PAYLOAD_BYTE + n] = ascii_byte(scalar)\\n                 n = n + 1\\n\",\"diff_bytes\":23089,\"diff_files\":14,\"diff_hunks\":15,\"diff_tail\":\"YLOAD_BYTE + n] = ascii_byte(scalar)\\n                 n = n + 1\\n\",\"id\":\"SO-21\",\"request_id\":\"pid-85189-1790266578-12000\",\"title\":\"Pin sawlang at 454207de (0.11.1): Sep 7 bump + slab borrows -> &var T + thread-preempt ';' split\"}","request_id":"pid-85189-1790266578-12000","sequence":43,"timestamp":"1790266592"}
---
