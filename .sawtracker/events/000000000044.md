---
{"action":"edit","actor":"agent:claude-sawos","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` names v0.12.0's version AND sha together"},{"checked_at":"","checked_by":"","text":"slab.saw `borrows -> &var T` + thread-preempt `;` fixes land"},{"checked_at":"","checked_by":"","text":"taskdump.saw's `__saw_rt_reactor_register` returns an honest nonzero Int status"},{"checked_at":"","checked_by":"","text":"`make sos-test` 382/382 at the new pin, transcript-diffed against the 0.11.1 baseline"},{"checked_at":"","checked_by":"","text":"`make sos-smoke-esp32c3` 3/3 (non-gating)"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790265383","description":"## Scope (RESCOPED Sep 24; the head syntax fixes moved to SO-21)\n\nThe **0.12.0** step only, stacked on SO-21 (which pins head 454207de at 0.11.1, per the user's ruling). SL-355 carries SAWC_VERSION 0.12.0 and changes `__saw_rt_reactor_register` from `void` to `word` (0, or the raw errno). Per the user ruling relayed in chat m159, sawos pins 0.12.0 and ports the export in the same move.\n\n## The change (built on branch `so-18-pin-0.12.0`, one commit on SO-21's tree)\n\n- `tests/taskdump.saw`: the export returns `ENOSYS` (38). SOS has no reactor, so nothing is ever armed. `0` would claim an arm that never happened and park a task forever; nonzero makes the executor's funnel panic, naming the fd. `task_dump` is a `--runtime-provider` gate case, so the signature check enforces this.\n- `tools/sos_runner.py`: the task_dump expectations' line numbers shift by 7.\n- `sawlang.pin`: to the 0.12.0 cut's version + sha together. This closes SO-21's untagged-sha note.\n\nProbed against SL-355.p1 r1 (c91bd7bf + the patch, tree fffd1895, a fresh clone): the ported file compiles on riscv32 and aarch64. The unported file fails (\"returns `void` where the ABI returns `word`\"). Head rejects the ported file (the mirror error). So the port and the pin must land together.\n\n## Waits on\n\nSL-355 merged, plus a 0.12.0 version (the tag is optional; the toolchain never reads tags). The Studio and Air sessions will ping in chat.\n\n## Acceptance\n\n- [ ] `sawlang.pin` names the 0.12.0 version AND its sha together\n- [ ] taskdump.saw's `__saw_rt_reactor_register` returns an honest nonzero Int status\n- [ ] `make sos-test` 382/382 at the new pin, transcript-diffed against SO-21's\n- [ ] `make sos-smoke-esp32c3` 3/3 (non-gating)\n","id":"SO-18","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":4,"sequence":44,"stage":"backlog","status":"open","title":"Advance the sawlang pin to 0.12.0: the SL-355 reactor_register ABI port","updated":"1790266639"},"before":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` names v0.12.0's version AND sha together"},{"checked_at":"","checked_by":"","text":"slab.saw `borrows -> &var T` + thread-preempt `;` fixes land"},{"checked_at":"","checked_by":"","text":"taskdump.saw's `__saw_rt_reactor_register` returns an honest nonzero Int status"},{"checked_at":"","checked_by":"","text":"`make sos-test` 382/382 at the new pin, transcript-diffed against the 0.11.1 baseline"},{"checked_at":"","checked_by":"","text":"`make sos-smoke-esp32c3` 3/3 (non-gating)"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790265383","description":"## Scope\n\nAdvance `sawlang.pin` from 0.11.1 (`6dce5cf1`) to the **0.12.0** cut, per the\nuser ruling relayed in chat m159: SL-355 carries SAWC_VERSION 0.12.0\n(BREAKING for runtime providers), and the merged gap since 0.11.1\n(SL-370/356/371, ...) rides in it with no separate cut. The pin's `version`\nand `sha` move TOGETHER, which answers claude-sos-reviewer's m158 point (1):\nat sawlang head today SAWC_VERSION still reads 0.11.1, so no pin can move\nuntil the 0.12.0 tag exists.\n\n## What sawlang head already needs (probed Sep 24 at `454207de`)\n\nThe full gate at head, against a fresh HTTPS clone (never `~/Projects/sawlang`),\nturned up two source breaks and nothing else:\n\n1. **`kernel/core/slab.saw:204`**: the `borrows` spelling now names the lent\n   reference and its mode. Verbatim:\n   `error: a \\`borrows\\` declaration names the LENT REFERENCE and its mode, not a returned value: write \\`borrows -> &T\\` if a use site may only READ through the place, or \\`borrows -> &var T\\` if it may also WRITE through it. \\`borrows -> T\\` says neither`.\n   Fix: `func [](&var self, i: Int) unsafe borrows -> &var T`. The spec's\n   receiver rule (`-> &var T` requires `&var self`) applies. The body only\n   reads `self` and lends, so the accessor stays shared-eligible: a read\n   through `EVENTS[i]` still borrows the slab shared.\n2. **`tests/thread-preempt/src/main.saw:157`**: two statements on one line\n   need a `;` now. Verbatim: `Parse error at 157:63: two statements on one line need a \\`;\\` between them: the first begins with \\`let\\`, the second with \\`return\\``.\n   Fix: the tree's multi-line catch-guard shape.\n\nA parse check of all 205 tracked `.saw` files with head's parser is clean\nafter (2). With both fixes the full gate at head passes: **382/382 = 129 + 129 + 124**.\nA transcript diff against a v0.11.1 baseline run is in progress.\n\n## What 0.12.0 adds (SL-355, not merged yet)\n\n`__saw_rt_reactor_register` changes from `void` to `word` (0, or the platform's\nraw errno; sawc/rt/ABI.md in SL-355.p1). `tests/taskdump.saw:42`'s export is a\nnever-called stub (SOS has no reactor), and `task_dump` is a\n`--runtime-provider` gate case, so the export must follow (m146, and item 4\nof codex's SL-355.p1 review). The honest status for \"there is no reactor on this machine\" is\nNONZERO. Returning 0 would claim the fd was armed, and a task would park\nforever. With a nonzero status the executor's funnel panics, naming the fd.\n\n## Acceptance\n\n- [ ] `sawlang.pin` names v0.12.0's version AND sha together\n- [ ] slab.saw `borrows -> &var T` + thread-preempt `;` fixes land\n- [ ] taskdump.saw's `__saw_rt_reactor_register` returns an honest nonzero Int status\n- [ ] `make sos-test` 382/382 at the new pin, transcript-diffed against the 0.11.1 baseline\n- [ ] `make sos-smoke-esp32c3` 3/3 (non-gating)\n","id":"SO-18","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":3,"sequence":26,"stage":"backlog","status":"open","title":"Advance the sawlang pin to 0.12.0: head's two syntax breaks and the SL-355 reactor_register ABI port","updated":"1790265389"},"id":"SO-18","project":"SO","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawos\",\"description\":\"## Scope (RESCOPED Sep 24; the head syntax fixes moved to SO-21)\\n\\nThe **0.12.0** step only, stacked on SO-21 (which pins head 454207de at 0.11.1, per the user's ruling). SL-355 carries SAWC_VERSION 0.12.0 and changes `__saw_rt_reactor_register` from `void` to `word` (0, or the raw errno). Per the user ruling relayed in chat m159, sawos pins 0.12.0 and ports the export in the same move.\\n\\n## The change (built on branch `so-18-pin-0.12.0`, one commit on SO-21's tree)\\n\\n- `tests/taskdump.saw`: the export returns `ENOSYS` (38). SOS has no reactor, so nothing is ever armed. `0` would claim an arm that never happened and park a task forever; nonzero makes the executor's funnel panic, naming the fd. `task_dump` is a `--runtime-provider` gate case, so the signature check enforces this.\\n- `tools/sos_runner.py`: the task_dump expectations' line numbers shift by 7.\\n- `sawlang.pin`: to the 0.12.0 cut's version + sha together. This closes SO-21's untagged-sha note.\\n\\nProbed against SL-355.p1 r1 (c91bd7bf + the patch, tree fffd1895, a fresh clone): the ported file compiles on riscv32 and aarch64. The unported file fails (\\\"returns `void` where the ABI returns `word`\\\"). Head rejects the ported file (the mirror error). So the port and the pin must land together.\\n\\n## Waits on\\n\\nSL-355 merged, plus a 0.12.0 version (the tag is optional; the toolchain never reads tags). The Studio and Air sessions will ping in chat.\\n\\n## Acceptance\\n\\n- [ ] `sawlang.pin` names the 0.12.0 version AND its sha together\\n- [ ] taskdump.saw's `__saw_rt_reactor_register` returns an honest nonzero Int status\\n- [ ] `make sos-test` 382/382 at the new pin, transcript-diffed against SO-21's\\n- [ ] `make sos-smoke-esp32c3` 3/3 (non-gating)\\n\",\"id\":\"SO-18\",\"request_id\":\"pid-85466-1790266627-16000\",\"title\":\"Advance the sawlang pin to 0.12.0: the SL-355 reactor_register ABI port\"}","request_id":"pid-85466-1790266627-16000","sequence":44,"timestamp":"1790266639"}
---
