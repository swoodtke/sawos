---
{"action":"edit","actor":"agent:claude-sawos","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note"},{"checked_at":"","checked_by":"","text":"The Sep 7 bump's content lands unchanged"},{"checked_at":"","checked_by":"","text":"The slab + thread-preempt fixes land"},{"checked_at":"","checked_by":"","text":"`make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain"}],"assignee":"","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790266548","description":"## Scope\n\nPin sawlang at current main **`454207de`**, which reports **0.11.1**. **User ruling (Sep 24, in session with claude-sawos): \"pin to head as 0.11.1.\"** No tag is needed. `tools/toolchain.py` never reads tags: the fetch path uses `sha`, and `version` is checked only against a `sawc` on `$PATH`. The version is honest, since head reports 0.11.1. That a real v0.11.1 `sawc` on `$PATH` also satisfies it is D-b2's documented asymmetry for an unreleased compiler, and the pin's comment now says so. That answers claude-sos-reviewer's m158 point (1).\n\nTwo things land together, because GitHub never received the first:\n\n1. **The Sep 7 bump, 0.8.0 → 0.11.1** (`aa9e88f` on the old local main, NEVER PUSHED; chat m167). It is replayed unchanged onto origin/main: its only difference from aa9e88f is ci.yml + .sawtracker. It carries SL-27's `sizeof` derive in pipe.saw, design 245's `chars()` → `scalars()` sweep (`sosrt.ascii_byte` takes a `Scalar`), and the designs/todo.md SL-26/27/28 records. It was gated 382/382 at v0.11.1 on Sep 7.\n2. **What head costs beyond 0.11.1.** A full gate at head found two source breaks and nothing else:\n   - `kernel/core/slab.saw` Slab.[]: `(&var self, i: Int) unsafe borrows -> &var T` (was `&self ... borrows -> T`). SL-333 makes the arrow name the lent reference and its mode, and `-> &var T` requires `&var self`. The body only lends, so the accessor stays SHARED-ELIGIBLE per the spec's \"Receiver access is a second fact\": a read through `EVENTS[i]` still borrows the slab shared. A doc comment says so.\n   - `tests/thread-preempt/src/main.saw:157`: SL-347 refuses two statements on one line with nothing between them. The fix uses the tree's multi-line catch-guard shape (the user's Sep 22 style ruling: one line or fully broken).\n\nA parse check of all 205 tracked `.saw` files with head's parser: clean.\n\n## Not in scope\n\nSL-355's reactor_register ABI (0.12.0) is not at head. SO-18 keeps that (taskdump.saw's ENOSYS port, then the pin to the 0.12.0 cut) and stacks on this.\n\n## Evidence\n\n- Full gate at head (SAWLANG_ROOT = fresh HTTPS clone at 454207de; aa9e88f + the two fixes): **382/382 = 129 + 129 + 124**.\n- Pinned FETCH path (no SAWLANG_ROOT, no sawc on PATH): `toolchain.py` resolves `via: fetch` and cold-clones 454207de with its venv.\n- In progress: the final gate on the exact patch tree via the fetch path, diffed against a v0.11.1 baseline transcript of aa9e88f. Results post here.\n\n## Acceptance\n\n- [ ] `sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\n- [ ] The Sep 7 bump's content lands unchanged\n- [ ] The slab + thread-preempt fixes land\n- [ ] `make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\n","id":"SO-21","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":2,"sequence":41,"stage":"backlog","status":"open","title":"Pin sawlang at head 454207de (reports 0.11.1): land the unpushed Sep 7 bump + head's two source fixes","updated":"1790266562"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790266548","description":"## Scope\n\nPin sawlang at current main **`454207de`**, which reports **0.11.1**. **User ruling (Sep 24, in session with claude-sawos): \"pin to head as 0.11.1.\"** No tag is needed. `tools/toolchain.py` never reads tags: the fetch path uses `sha`, and `version` is checked only against a `sawc` on `$PATH`. The version is honest, since head reports 0.11.1. That a real v0.11.1 `sawc` on `$PATH` also satisfies it is D-b2's documented asymmetry for an unreleased compiler, and the pin's comment now says so. That answers claude-sos-reviewer's m158 point (1).\n\nTwo things land together, because GitHub never received the first:\n\n1. **The Sep 7 bump, 0.8.0 → 0.11.1** (`aa9e88f` on the old local main, NEVER PUSHED; chat m167). It is replayed unchanged onto origin/main: its only difference from aa9e88f is ci.yml + .sawtracker. It carries SL-27's `sizeof` derive in pipe.saw, design 245's `chars()` → `scalars()` sweep (`sosrt.ascii_byte` takes a `Scalar`), and the designs/todo.md SL-26/27/28 records. It was gated 382/382 at v0.11.1 on Sep 7.\n2. **What head costs beyond 0.11.1.** A full gate at head found two source breaks and nothing else:\n   - `kernel/core/slab.saw` Slab.[]: `(&var self, i: Int) unsafe borrows -> &var T` (was `&self ... borrows -> T`). SL-333 makes the arrow name the lent reference and its mode, and `-> &var T` requires `&var self`. The body only lends, so the accessor stays SHARED-ELIGIBLE per the spec's \"Receiver access is a second fact\": a read through `EVENTS[i]` still borrows the slab shared. A doc comment says so.\n   - `tests/thread-preempt/src/main.saw:157`: SL-347 refuses two statements on one line with nothing between them. The fix uses the tree's multi-line catch-guard shape (the user's Sep 22 style ruling: one line or fully broken).\n\nA parse check of all 205 tracked `.saw` files with head's parser: clean.\n\n## Not in scope\n\nSL-355's reactor_register ABI (0.12.0) is not at head. SO-18 keeps that (taskdump.saw's ENOSYS port, then the pin to the 0.12.0 cut) and stacks on this.\n\n## Evidence\n\n- Full gate at head (SAWLANG_ROOT = fresh HTTPS clone at 454207de; aa9e88f + the two fixes): **382/382 = 129 + 129 + 124**.\n- Pinned FETCH path (no SAWLANG_ROOT, no sawc on PATH): `toolchain.py` resolves `via: fetch` and cold-clones 454207de with its venv.\n- In progress: the final gate on the exact patch tree via the fetch path, diffed against a v0.11.1 baseline transcript of aa9e88f. Results post here.\n\n## Acceptance\n\n- [ ] `sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\n- [ ] The Sep 7 bump's content lands unchanged\n- [ ] The slab + thread-preempt fixes land\n- [ ] `make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\n","id":"SO-21","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":1,"sequence":40,"stage":"backlog","status":"open","title":"Pin sawlang at head 454207de (reports 0.11.1): land the unpushed Sep 7 bump + head's two source fixes","updated":"1790266548"},"id":"SO-21","project":"SO","request":"{\"acceptance\":[{\"checked\":false,\"text\":\"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\"},{\"checked\":false,\"text\":\"The Sep 7 bump's content lands unchanged\"},{\"checked\":false,\"text\":\"The slab + thread-preempt fixes land\"},{\"checked\":false,\"text\":\"`make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\"}],\"action\":\"edit\",\"actor\":\"agent:claude-sawos\",\"id\":\"SO-21\",\"request_id\":\"pid-85022-1790266548-12000\"}","request_id":"pid-85022-1790266548-12000","sequence":41,"timestamp":"1790266562"}
---
