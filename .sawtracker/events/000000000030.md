---
{"action":"patch_add","actor":"agent:claude-sawos","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"ci.yml triggers on workflow_dispatch only"},{"checked_at":"","checked_by":"","text":"no push to main triggers an Actions run after this merges"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[{"author":"agent:claude-sawos","body":"Submitted patch SO-19.p1 (revision 1): Disable CI's push/pull_request triggers; keep workflow_dispatch","created":"1790265639","id":"c1"}],"created":"1790265633","description":"## Scope\n\nUser ruling (Sep 24 2026): disable the GitHub Actions workflow that builds and runs the tests. The gate moves to the sawtracker server, which will run `make sos-test` on SO patches when SO's test command is enabled there (SO has no `.sawtracker/tests.json` today).\n\n## Change\n\n`.github/workflows/ci.yml`: the `push` and `pull_request` triggers are replaced by `workflow_dispatch` alone, with a comment giving the ruling. Nothing runs automatically any more. The job itself is untouched and can still be run by hand from the Actions tab. That matters for SO-11: this workflow is the only cold run of the pinned FETCH path (tools/toolchain.py step 3), and SO-11's acceptance is exactly that run.\n\nNot changed: tools/toolchain.py's docstring still names CI as an entry point. That file travels verbatim from sawlang (its own docstring says so), so it is not edited per repository. The \"never CI\" lines in CLAUDE.md, the Makefile and sos_runner (the esp32c3 smoke) stay true.\n\n## Acceptance\n\n- [ ] ci.yml triggers on workflow_dispatch only\n- [ ] no push to main triggers an Actions run after this merges\n","id":"SO-19","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":4,"sequence":30,"stage":"backlog","status":"open","title":"Disable the GitHub Actions test workflow; the gate moves to sawtracker","updated":"1790265639"},"before":{"acceptance":[{"checked_at":"","checked_by":"","text":"ci.yml triggers on workflow_dispatch only"},{"checked_at":"","checked_by":"","text":"no push to main triggers an Actions run after this merges"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790265633","description":"## Scope\n\nUser ruling (Sep 24 2026): disable the GitHub Actions workflow that builds and runs the tests. The gate moves to the sawtracker server, which will run `make sos-test` on SO patches when SO's test command is enabled there (SO has no `.sawtracker/tests.json` today).\n\n## Change\n\n`.github/workflows/ci.yml`: the `push` and `pull_request` triggers are replaced by `workflow_dispatch` alone, with a comment giving the ruling. Nothing runs automatically any more. The job itself is untouched and can still be run by hand from the Actions tab. That matters for SO-11: this workflow is the only cold run of the pinned FETCH path (tools/toolchain.py step 3), and SO-11's acceptance is exactly that run.\n\nNot changed: tools/toolchain.py's docstring still names CI as an entry point. That file travels verbatim from sawlang (its own docstring says so), so it is not edited per repository. The \"never CI\" lines in CLAUDE.md, the Makefile and sos_runner (the esp32c3 smoke) stay true.\n\n## Acceptance\n\n- [ ] ci.yml triggers on workflow_dispatch only\n- [ ] no push to main triggers an Actions run after this merges\n","id":"SO-19","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":3,"sequence":29,"stage":"backlog","status":"open","title":"Disable the GitHub Actions test workflow; the gate moves to sawtracker","updated":"1790265638"},"id":"SO-19","project":"SO","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawos\",\"diff\":\"diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml\\nindex 46395aa..c066c9f 100644\\n--- a/.github/workflows/ci.yml\\n+++ b/.github/workflows/ci.yml\\n@@ -1,8 +1,12 @@\\n name: CI\\n \\n+# DISABLED (user ruling, Sep 24 2026): the gate moves to the sawtracker\\n+# server, which will run `make sos-test` on SO patches once SO's test_command\\n+# is enabled there. Push and pull_request no longer trigger this; it stays\\n+# runnable by hand (Actions tab, \\\"Run workflow\\\") because it is the one place\\n+# the pinned FETCH path (toolchain.py step 3) runs cold, which SO-11 needs.\\n on:\\n-  push:\\n-  pull_request:\\n+  workflow_dispatch:\\n \\n jobs:\\n   sos-test:\\n\",\"diff_bytes\":644,\"diff_files\":1,\"diff_hunks\":1,\"diff_tail\":\"h:\\n-  pull_request:\\n+  workflow_dispatch:\\n \\n jobs:\\n   sos-test:\\n\",\"id\":\"SO-19\",\"request_id\":\"pid-80339-1790265638-14000\",\"title\":\"Disable CI's push/pull_request triggers; keep workflow_dispatch\"}","request_id":"pid-80339-1790265638-14000","sequence":30,"timestamp":"1790265639"}
---
