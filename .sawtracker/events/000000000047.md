---
{"action":"patch_add","actor":"agent:claude-sos-reviewer","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"SO patches get a server test run (`tests: passed/failed` on `patch show`)"},{"checked_at":"","checked_by":"","text":"the command is `make sos-test` or a ruled quicker lane, stated in the file"}],"assignee":"","author":"agent:claude-sawos","closed":"","comments":[{"author":"agent:claude-sos-reviewer","body":"claude-sos-reviewer: answers to this issue's open questions, and the patch that follows (SO-20.p1).\n\n**The user's direction (human:shawn, Sep 24, in session with claude-sos-reviewer), verbatim.** The tracker refuses a ruling from an agent, so this is a quote, not a `--kind ruling`. The user may record it as one.\n> \"we should enable sawtracker to run the final gate so we only need to run targetted tests per patch.\"\n> \"go ahead and add your comments to SL-20 [= SO-20] and implement your changes. you have permission to add the tests.json file in the .sawtracker directory. we can continue to share the work queue with the other projects in sawtracker server for now and adjust if necessary. though we shouldn't limit the server to 4 cpus.\"\n\n**Open questions, answered**\n1. *Who lands tests.json:* the user permits it in an ordinary patch (above). Per sawtracker's README, a patch that introduces the file is gated immediately by its own command, so SO-20.p1's first server run is also the first test of the setup.\n2. *Server toolchain (the Studio; the server owner's to install):* QEMU for riscv32 and aarch64, Homebrew LLVM `clang` (or `SOS_CLANG`) plus `ld.lld`, and python3. No `SAWLANG_ROOT`: `toolchain.py` step 3 cold-fetches `sawlang.pin`'s sha and builds its venv, which needs network to GitHub and PyPI from the test run. `SAW_TOOLCHAIN_CACHE` at server launch picks where fetched checkouts are cached. A side benefit is that every run exercises SO-11's cold-fetch path, and a pin bump needs no server change.\n3. *Duration and quicker lane:* no quicker lane. The server runs the whole gate, and local runs become the quick lane (`--case`/`--arch`). The queue is ONE queue for all projects (`test_runner` in sawtracker `src/main.saw` drains a single channel), so an SO gate delays SL runs and vice versa. The user accepts that for now. sawtracker has no hard timeout; the runner's 10 s per-case QEMU timeout bounds a hung guest, but a wedged `sawc` would stall the queue.\n4. *Parallelism:* `-j 4` was the Air's P-core count. The runner now DETECTS the host's performance cores (perflevel 0; every core where no such key exists), so it is still 4 on the Air, unchanged. `SOS_JOBS` overrides at server launch and `-j` beats both. The job count is never printed, so a server transcript and a local one diff cleanly.\n5. *Suite lock:* the server's runs are on the Studio and do not take the Air's `sawos-suite-lock`. It matters only if someone also runs sawos gates by hand on the Studio.\n\n**Review flow once this merges:** the verdict comes from `patch show` and `patch log SO-N.pK` is the transcript; the oracle diff is that log against the last merged patch's log. A patch based before SO-20 carries no tests.json in its base or its tree, so it runs UNGATED until it is rebased. **SO-21.p1 needs `patch rebase` after SO-20 merges.** SO-20.p1's own passing log doubles as the 0.8.0 main transcript SO-21's diff wants: the runner change prints nothing new.\n\n**Verification done here:** `py_compile`; `_default_jobs()` returns 4 on the Air with SOS_JOBS unset or empty, and SOS_JOBS=2/16 returns 2/16; SOS_JOBS=0/abc/-3/' 4' are each refused with exit 2; `main()` driven through the resolution (`--arch nope` with no `-j`, `-j 3`, `-j 0`, SOS_JOBS=x) takes the expected path each time. NOT run: a QEMU case locally, because the Air's suite lock is held by claude-sawos's gate. The server run on this patch is the end-to-end proof, and that is the point of the change.\n\n**Authorship:** written by the reviewer at the user's direction, so it needs a reviewer other than me: claude-sawos or human:shawn. If the Studio lacks a tool, the first run fails fast naming it; install it, then `patch retest SO-20.p1`.","created":"1790271709","id":"c1"},{"author":"agent:claude-sos-reviewer","body":"Submitted patch SO-20.p1 (revision 1): Server-side gate: .sawtracker/tests.json runs make sos-test; runner -j tracks the host's P-cores (SOS_JOBS override)","created":"1790271710","id":"c2"}],"created":"1790265840","description":"## Scope\n\nGive SO a server-side gate: a `.sawtracker/tests.json` (sawlang has one) naming `make sos-test`, so the sawtracker server runs the gate on every SO patch the way it does on SL patches. Filed at claude-sos-reviewer's request (SO-19 c2, note 1).\n\n## Why now\n\nSO-19 (merged b6d9d4d) turned off GitHub Actions' push/pull_request triggers. Until this lands, NOTHING runs the gate automatically on an SO patch. The reviewer's own `make sos-test` run on base + patch is the only gate. This issue bounds that interim.\n\nFor the record, the Actions gate was not doing the job anyway. 8 of the 9 runs in the repository's history FAILED at the `sos-test` step (exit 2), including every run since Sep 2. The one success was 77b0870 on Sep 1. The logs need GitHub auth, so the cause is undiagnosed here (see SO-11).\n\n## Open questions (the user's / the server owner's)\n\n- `.sawtracker/` is the SERVER's to modify (standing rule), so the file presumably lands through the server or its owner, not an SO patch. Say which.\n- The server host needs the gate's toolchain: QEMU (riscv32 + aarch64), LLVM clang + lld, and a sawlang checkout at `sawlang.pin`'s sha (SAWLANG_ROOT, or the pinned fetch into `~/.cache/saw-toolchain`).\n- The gate runs about 60 minutes at `-j 4` for all three profiles (Sep 24, this host). A per-patch run may want a quicker lane, as sawlang's tier-1/tier-2 ruling did (m104 point 5).\n- The suite lock: server runs must share the machine-wide sawos lock (`/private/tmp/claude-<uid>/sawos-suite-lock`) with interactive gate runs.\n\n## Acceptance\n\n- [ ] SO patches get a server test run (`tests: passed/failed` on `patch show`)\n- [ ] the command is `make sos-test` or a ruled quicker lane, stated in the file\n","id":"SO-20","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":4,"sequence":47,"stage":"backlog","status":"open","title":"Give SO a server-side gate (.sawtracker/tests.json running make sos-test)","updated":"1790271710"},"before":{"acceptance":[{"checked_at":"","checked_by":"","text":"SO patches get a server test run (`tests: passed/failed` on `patch show`)"},{"checked_at":"","checked_by":"","text":"the command is `make sos-test` or a ruled quicker lane, stated in the file"}],"assignee":"","author":"agent:claude-sawos","closed":"","comments":[{"author":"agent:claude-sos-reviewer","body":"claude-sos-reviewer: answers to this issue's open questions, and the patch that follows (SO-20.p1).\n\n**The user's direction (human:shawn, Sep 24, in session with claude-sos-reviewer), verbatim.** The tracker refuses a ruling from an agent, so this is a quote, not a `--kind ruling`. The user may record it as one.\n> \"we should enable sawtracker to run the final gate so we only need to run targetted tests per patch.\"\n> \"go ahead and add your comments to SL-20 [= SO-20] and implement your changes. you have permission to add the tests.json file in the .sawtracker directory. we can continue to share the work queue with the other projects in sawtracker server for now and adjust if necessary. though we shouldn't limit the server to 4 cpus.\"\n\n**Open questions, answered**\n1. *Who lands tests.json:* the user permits it in an ordinary patch (above). Per sawtracker's README, a patch that introduces the file is gated immediately by its own command, so SO-20.p1's first server run is also the first test of the setup.\n2. *Server toolchain (the Studio; the server owner's to install):* QEMU for riscv32 and aarch64, Homebrew LLVM `clang` (or `SOS_CLANG`) plus `ld.lld`, and python3. No `SAWLANG_ROOT`: `toolchain.py` step 3 cold-fetches `sawlang.pin`'s sha and builds its venv, which needs network to GitHub and PyPI from the test run. `SAW_TOOLCHAIN_CACHE` at server launch picks where fetched checkouts are cached. A side benefit is that every run exercises SO-11's cold-fetch path, and a pin bump needs no server change.\n3. *Duration and quicker lane:* no quicker lane. The server runs the whole gate, and local runs become the quick lane (`--case`/`--arch`). The queue is ONE queue for all projects (`test_runner` in sawtracker `src/main.saw` drains a single channel), so an SO gate delays SL runs and vice versa. The user accepts that for now. sawtracker has no hard timeout; the runner's 10 s per-case QEMU timeout bounds a hung guest, but a wedged `sawc` would stall the queue.\n4. *Parallelism:* `-j 4` was the Air's P-core count. The runner now DETECTS the host's performance cores (perflevel 0; every core where no such key exists), so it is still 4 on the Air, unchanged. `SOS_JOBS` overrides at server launch and `-j` beats both. The job count is never printed, so a server transcript and a local one diff cleanly.\n5. *Suite lock:* the server's runs are on the Studio and do not take the Air's `sawos-suite-lock`. It matters only if someone also runs sawos gates by hand on the Studio.\n\n**Review flow once this merges:** the verdict comes from `patch show` and `patch log SO-N.pK` is the transcript; the oracle diff is that log against the last merged patch's log. A patch based before SO-20 carries no tests.json in its base or its tree, so it runs UNGATED until it is rebased. **SO-21.p1 needs `patch rebase` after SO-20 merges.** SO-20.p1's own passing log doubles as the 0.8.0 main transcript SO-21's diff wants: the runner change prints nothing new.\n\n**Verification done here:** `py_compile`; `_default_jobs()` returns 4 on the Air with SOS_JOBS unset or empty, and SOS_JOBS=2/16 returns 2/16; SOS_JOBS=0/abc/-3/' 4' are each refused with exit 2; `main()` driven through the resolution (`--arch nope` with no `-j`, `-j 3`, `-j 0`, SOS_JOBS=x) takes the expected path each time. NOT run: a QEMU case locally, because the Air's suite lock is held by claude-sawos's gate. The server run on this patch is the end-to-end proof, and that is the point of the change.\n\n**Authorship:** written by the reviewer at the user's direction, so it needs a reviewer other than me: claude-sawos or human:shawn. If the Studio lacks a tool, the first run fails fast naming it; install it, then `patch retest SO-20.p1`.","created":"1790271709","id":"c1"}],"created":"1790265840","description":"## Scope\n\nGive SO a server-side gate: a `.sawtracker/tests.json` (sawlang has one) naming `make sos-test`, so the sawtracker server runs the gate on every SO patch the way it does on SL patches. Filed at claude-sos-reviewer's request (SO-19 c2, note 1).\n\n## Why now\n\nSO-19 (merged b6d9d4d) turned off GitHub Actions' push/pull_request triggers. Until this lands, NOTHING runs the gate automatically on an SO patch. The reviewer's own `make sos-test` run on base + patch is the only gate. This issue bounds that interim.\n\nFor the record, the Actions gate was not doing the job anyway. 8 of the 9 runs in the repository's history FAILED at the `sos-test` step (exit 2), including every run since Sep 2. The one success was 77b0870 on Sep 1. The logs need GitHub auth, so the cause is undiagnosed here (see SO-11).\n\n## Open questions (the user's / the server owner's)\n\n- `.sawtracker/` is the SERVER's to modify (standing rule), so the file presumably lands through the server or its owner, not an SO patch. Say which.\n- The server host needs the gate's toolchain: QEMU (riscv32 + aarch64), LLVM clang + lld, and a sawlang checkout at `sawlang.pin`'s sha (SAWLANG_ROOT, or the pinned fetch into `~/.cache/saw-toolchain`).\n- The gate runs about 60 minutes at `-j 4` for all three profiles (Sep 24, this host). A per-patch run may want a quicker lane, as sawlang's tier-1/tier-2 ruling did (m104 point 5).\n- The suite lock: server runs must share the machine-wide sawos lock (`/private/tmp/claude-<uid>/sawos-suite-lock`) with interactive gate runs.\n\n## Acceptance\n\n- [ ] SO patches get a server test run (`tests: passed/failed` on `patch show`)\n- [ ] the command is `make sos-test` or a ruled quicker lane, stated in the file\n","id":"SO-20","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":3,"sequence":46,"stage":"backlog","status":"open","title":"Give SO a server-side gate (.sawtracker/tests.json running make sos-test)","updated":"1790271709"},"id":"SO-20","project":"SO","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sos-reviewer\",\"diff\":\"diff --git a/.sawtracker/tests.json b/.sawtracker/tests.json\\nnew file mode 100644\\nindex 0000000..8864025\\n--- /dev/null\\n+++ b/.sawtracker/tests.json\\n@@ -0,0 +1 @@\\n+{\\\"command\\\": [\\\"make\\\", \\\"sos-test\\\"]}\\ndiff --git a/CLAUDE.md b/CLAUDE.md\\nindex 73c422d..37cc08c 100644\\n--- a/CLAUDE.md\\n+++ b/CLAUDE.md\\n@@ -63,12 +63,29 @@ Every change gates on `make sos-test` before commit. The acceptance oracle\\n tradition: diff the console transcript, don't just read \\\"green\\\"\\n (sawlang#238 unit 0).\\n \\n+**The sawtracker server runs that gate on every SO patch** (SO-20, Sep 24):\\n+`.sawtracker/tests.json` names `make sos-test`, run in a disposable\\n+worktree at the patch's applied commit, on each submit and revise and on\\n+`patch retest`, with sawlang resolved by `toolchain.py`'s pinned fetch.\\n+`patch show` carries the verdict and `patch log SO-N.pK` the full\\n+transcript, so the oracle diff is the patch's log against the last merged\\n+patch's. A merge waits for a green run of the revision that lands. The\\n+server's test queue is shared with every tracker project (user, Sep 24:\\n+fine for now, adjust if it bites). With the server running the whole\\n+gate, a local run can be the cases a change touches (`--case`, `--arch`);\\n+a change that reaches every case (a pin bump, the kernel core, a HAL, the\\n+runner) waits on the server's run rather than repeating it here.\\n+\\n `make sos-smoke-esp32c3` (design 20, stubbed) is NON-GATING board\\n smoke against the machine-local Espressif QEMU — never part of\\n sos-test, never CI.\\n \\n-The runner is PARALLEL BY DEFAULT — `-j 4`, a user ruling (Sep 1: four\\n-P-cores, six E-cores; the parallelism tracks the P-cores). The report is\\n+The runner is PARALLEL BY DEFAULT — one job per PERFORMANCE core, a user\\n+ruling (Sep 1: the parallelism tracks the P-cores — four on the Air, which\\n+has six E-cores beside them). The count is detected at startup so the\\n+server's host gets its own (user, Sep 24: the server is not limited to\\n+four); `SOS_JOBS` overrides the detection and `-j N` overrides both. The\\n+job count is never printed, so it cannot move a transcript. The report is\\n printed in case-definition order whatever the completion order, so a\\n transcript diff reads the same as it always did. `-j 1` is the serial\\n harness, kept reachable for exactly that comparison. THREE CASES CARRY\\ndiff --git a/tools/sos_runner.py b/tools/sos_runner.py\\nindex c96c07a..76d2ffc 100644\\n--- a/tools/sos_runner.py\\n+++ b/tools/sos_runner.py\\n@@ -5355,6 +5355,40 @@ def _map_ordered(jobs, items, work):\\n                 fut.cancel()\\n \\n \\n+def _default_jobs():\\n+    \\\"\\\"\\\"How many package builds, and then cases, run at once when `-j` is absent.\\n+\\n+    ONE PER PERFORMANCE CORE — the user's ruling (Sep 1: the parallelism tracks\\n+    the P-cores; four on the MacBook Air it was ruled on). The count is DETECTED\\n+    rather than written down, because the sawtracker server runs this same\\n+    command on every SO patch (SO-20) on a bigger host, and the ruling is about\\n+    the host's cores, not about the number four (user, Sep 24: the server is not\\n+    to be limited to four). Apple silicon reports its performance cores as\\n+    perflevel 0; a host with no such key (Linux, an Intel Mac) has one kind of\\n+    core and gets all of them.\\n+\\n+    `SOS_JOBS` overrides the detection, for a host whose cores are shared with\\n+    other work; `-j` overrides both. A malformed `SOS_JOBS` is refused rather\\n+    than ignored — a value that silently fell back would be a run nobody asked\\n+    for.\\n+    \\\"\\\"\\\"\\n+    named = os.environ.get(\\\"SOS_JOBS\\\")\\n+    if named:\\n+        if not named.isdigit() or int(named) < 1:\\n+            print(f\\\"{RED}SOS_JOBS must be a whole number, at least 1 \\\"\\n+                  f\\\"(got {named!r}){RESET}\\\", file=sys.stderr)\\n+            sys.exit(2)\\n+        return int(named)\\n+    try:\\n+        probe = subprocess.run([\\\"sysctl\\\", \\\"-n\\\", \\\"hw.perflevel0.physicalcpu\\\"],\\n+                               capture_output=True, text=True)\\n+    except OSError:\\n+        probe = None\\n+    if probe is not None and probe.returncode == 0 and probe.stdout.strip().isdigit():\\n+        return max(1, int(probe.stdout.strip()))\\n+    return os.cpu_count() or 1\\n+\\n+\\n def _find_clang(arches):\\n     \\\"\\\"\\\"Return the first clang that can assemble EVERY target's boot code.\\n \\n@@ -6184,10 +6218,11 @@ def main():\\n                              \\\"comma-separated list; hyphens and underscores are \\\"\\n                              \\\"interchangeable. A DEVELOPMENT convenience: the \\\"\\n                              \\\"GATE is every case on every architecture\\\")\\n-    parser.add_argument(\\\"-j\\\", \\\"--jobs\\\", metavar=\\\"N\\\", type=int, default=4,\\n+    parser.add_argument(\\\"-j\\\", \\\"--jobs\\\", metavar=\\\"N\\\", type=int, default=None,\\n                         help=\\\"run N package builds, and then N cases, at a \\\"\\n-                             \\\"time (default: 4 — this host's performance-core \\\"\\n-                             \\\"count; `-j 1` is the serial harness)\\\")\\n+                             \\\"time (default: this host's performance-core \\\"\\n+                             \\\"count, or SOS_JOBS when set; `-j 1` is the \\\"\\n+                             \\\"serial harness)\\\")\\n     parser.add_argument(\\\"--board\\\", metavar=\\\"NAME\\\", default=\\\"virt\\\",\\n                         help=\\\"board profile (default: virt — the GATE's, and \\\"\\n                              \\\"the only implemented one). `esp32c3` is design \\\"\\n@@ -6559,7 +6594,9 @@ def main():\\n         print(\\\"=\\\" * 60)\\n         sys.exit(1 if failed else 0)\\n \\n-    if args.jobs < 1:\\n+    if args.jobs is None:\\n+        args.jobs = _default_jobs()\\n+    elif args.jobs < 1:\\n         print(f\\\"{RED}-j must be at least 1 (got {args.jobs}){RESET}\\\",\\n               file=sys.stderr)\\n         sys.exit(2)\\n\",\"diff_bytes\":5692,\"diff_files\":3,\"diff_hunks\":5,\"diff_tail\":\"){RESET}\\\",\\n               file=sys.stderr)\\n         sys.exit(2)\\n\",\"id\":\"SO-20\",\"request_id\":\"pid-5819-1790271710-13000\",\"title\":\"Server-side gate: .sawtracker/tests.json runs make sos-test; runner -j tracks the host's P-cores (SOS_JOBS override)\"}","request_id":"pid-5819-1790271710-13000","sequence":47,"timestamp":"1790271710"}
---
