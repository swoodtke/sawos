---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791288","description":"## Scope and status\n\nVerify and adopt the compound-static repeat-value fix in the SawOS pin\n\nThe Sawlang TODO records the upstream resolution: DF‑307a const-eval leaf unification, closing DF‑294c and DF‑300c. Verify the current SawOS pin and the original probe, then remove only the now-unnecessary workaround and run affected target tests. This is downstream adoption, not a new upstream compiler defect.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Re-run the original minimal probe or acceptance case at the current revision/pin; record compiler version, target, command and observed result.\n2. Compare the result with the later ruling, fix and regression coverage. Close with evidence if resolved; otherwise minimize the surviving case and scope its fix.\n3. For a SawOS pin-adoption task, update only the proven-obsolete workaround, then run its affected QEMU cases on each supported target.\n\n## Acceptance criteria\n\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawos TODO lines 347–358\n\n[Original record](https://github.com/swoodtke/sawos/blob/754ec9570aef54609820cc7e28457c7623547670/designs/todo.md#L347-L358)\n\n- SL‑21 — A STRUCT-TYPED `static` MAY NOT BE A REPEAT LITERAL'S VALUE, THOUGH AN INTEGER-TYPED ONE MAY, AND THOUGH THE SAME STRUCT LITERAL WRITTEN INLINE COMPILES: ``static `C` must be initialized by a compile-time constant`` (design 32, `kernel/core/waitables.saw`, sawc 0.4.0 @ `46eebb36`). The hint that comes with it lists \"an earlier module `static`\" among the things a constant expression may name, which is what makes this look like a bug rather than a rule — the entry is a `static` and it IS earlier. Minimal repro, one file, hosted:\n  ```saw\n  struct Slot { a: Int, b: Int }\n  static N: Int = 4\n  static ZERO_INT: Int = 0\n  static ZERO_SLOT: Slot = Slot(a: 0, b: 0)\n\n  unsafe static var A: [Int; N] = [ZERO_INT; N]          // compiles\n  unsafe static var B: [Slot; N] = [Slot(a: 0, b: 0); N] // compiles\n  unsafe static var C: [Slot; N] = [ZERO_SLOT; N]        // error\n  ```\n  So the repeat literal's VALUE position accepts a struct literal and an integer static but not a struct static, and the three are equally constant. It bites where a slab's zero element is wanted by name: design 32 converts nine kernel slabs whose initializers each repeat a zero slot, and naming that slot once per kind (`static ZERO_EVENT: EventSlot = ...`) is exactly what the size-in-one-place idiom would suggest. Workaround in-tree: write the struct literal INLINE inside each repeat, which is what the arrays already did before the conversion, so nothing regressed — but the nine initializers now repeat their field lists in the one place a name would have read better. Resolution: let a repeat literal's value name a `static` of any constant-constructible type, exactly as it names an integer one — or, if the restriction is deliberate, say which types the value position admits and make the hint stop advertising \"an earlier module `static`\" for a case it does not accept.\n","id":"SO-15","labels":["todo-import","pin-bump","needs-verification","verification","plan"],"priority":"normal","project":"SO","revision":1,"sequence":15,"status":"open","title":"Verify and adopt the compound-static repeat-value fix in the SawOS pin","updated":"1788791288"},"before":null,"id":"SO-15","project":"SO","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nVerify and adopt the compound-static repeat-value fix in the SawOS pin\\n\\nThe Sawlang TODO records the upstream resolution: DF‑307a const-eval leaf unification, closing DF‑294c and DF‑300c. Verify the current SawOS pin and the original probe, then remove only the now-unnecessary workaround and run affected target tests. This is downstream adoption, not a new upstream compiler defect.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Re-run the original minimal probe or acceptance case at the current revision/pin; record compiler version, target, command and observed result.\\n2. Compare the result with the later ruling, fix and regression coverage. Close with evidence if resolved; otherwise minimize the surviving case and scope its fix.\\n3. For a SawOS pin-adoption task, update only the proven-obsolete workaround, then run its affected QEMU cases on each supported target.\\n\\n## Acceptance criteria\\n\\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawos TODO lines 347–358\\n\\n[Original record](https://github.com/swoodtke/sawos/blob/754ec9570aef54609820cc7e28457c7623547670/designs/todo.md#L347-L358)\\n\\n- SL‑21 — A STRUCT-TYPED `static` MAY NOT BE A REPEAT LITERAL'S VALUE, THOUGH AN INTEGER-TYPED ONE MAY, AND THOUGH THE SAME STRUCT LITERAL WRITTEN INLINE COMPILES: ``static `C` must be initialized by a compile-time constant`` (design 32, `kernel/core/waitables.saw`, sawc 0.4.0 @ `46eebb36`). The hint that comes with it lists \\\"an earlier module `static`\\\" among the things a constant expression may name, which is what makes this look like a bug rather than a rule — the entry is a `static` and it IS earlier. Minimal repro, one file, hosted:\\n  ```saw\\n  struct Slot { a: Int, b: Int }\\n  static N: Int = 4\\n  static ZERO_INT: Int = 0\\n  static ZERO_SLOT: Slot = Slot(a: 0, b: 0)\\n\\n  unsafe static var A: [Int; N] = [ZERO_INT; N]          // compiles\\n  unsafe static var B: [Slot; N] = [Slot(a: 0, b: 0); N] // compiles\\n  unsafe static var C: [Slot; N] = [ZERO_SLOT; N]        // error\\n  ```\\n  So the repeat literal's VALUE position accepts a struct literal and an integer static but not a struct static, and the three are equally constant. It bites where a slab's zero element is wanted by name: design 32 converts nine kernel slabs whose initializers each repeat a zero slot, and naming that slot once per kind (`static ZERO_EVENT: EventSlot = ...`) is exactly what the size-in-one-place idiom would suggest. Workaround in-tree: write the struct literal INLINE inside each repeat, which is what the arrays already did before the conversion, so nothing regressed — but the nine initializers now repeat their field lists in the one place a name would have read better. Resolution: let a repeat literal's value name a `static` of any constant-constructible type, exactly as it names an integer one — or, if the restriction is deliberate, say which types the value position admits and make the hint stop advertising \\\"an earlier module `static`\\\" for a case it does not accept.\\n\",\"labels\":[\"todo-import\",\"pin-bump\",\"needs-verification\",\"verification\",\"plan\"],\"priority\":\"normal\",\"project\":\"SO\",\"request_id\":\"todo-20260907-ab7ce54b2fb89d9c2b7c-create\",\"title\":\"Verify and adopt the compound-static repeat-value fix in the SawOS pin\"}","request_id":"todo-20260907-ab7ce54b2fb89d9c2b7c-create","sequence":15,"timestamp":"1788791288"}
---
