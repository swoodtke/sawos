---
{"action":"edit","actor":"agent:claude-sawos","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"ci.yml triggers on workflow_dispatch only"},{"checked_at":"","checked_by":"","text":"no push to main triggers an Actions run after this merges"}],"assignee":"","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790265633","description":"## Scope\n\nUser ruling (Sep 24 2026): disable the GitHub Actions workflow that builds and runs the tests. The gate moves to the sawtracker server, which will run `make sos-test` on SO patches when SO's test command is enabled there (SO has no `.sawtracker/tests.json` today).\n\n## Change\n\n`.github/workflows/ci.yml`: the `push` and `pull_request` triggers are replaced by `workflow_dispatch` alone, with a comment giving the ruling. Nothing runs automatically any more. The job itself is untouched and can still be run by hand from the Actions tab. That matters for SO-11: this workflow is the only cold run of the pinned FETCH path (tools/toolchain.py step 3), and SO-11's acceptance is exactly that run.\n\nNot changed: tools/toolchain.py's docstring still names CI as an entry point. That file travels verbatim from sawlang (its own docstring says so), so it is not edited per repository. The \"never CI\" lines in CLAUDE.md, the Makefile and sos_runner (the esp32c3 smoke) stay true.\n\n## Acceptance\n\n- [ ] ci.yml triggers on workflow_dispatch only\n- [ ] no push to main triggers an Actions run after this merges\n","id":"SO-19","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":2,"sequence":28,"stage":"backlog","status":"open","title":"Disable the GitHub Actions test workflow; the gate moves to sawtracker","updated":"1790265638"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawos","closed":"","comments":[],"created":"1790265633","description":"## Scope\n\nUser ruling (Sep 24 2026): disable the GitHub Actions workflow that builds and runs the tests. The gate moves to the sawtracker server, which will run `make sos-test` on SO patches when SO's test command is enabled there (SO has no `.sawtracker/tests.json` today).\n\n## Change\n\n`.github/workflows/ci.yml`: the `push` and `pull_request` triggers are replaced by `workflow_dispatch` alone, with a comment giving the ruling. Nothing runs automatically any more. The job itself is untouched and can still be run by hand from the Actions tab. That matters for SO-11: this workflow is the only cold run of the pinned FETCH path (tools/toolchain.py step 3), and SO-11's acceptance is exactly that run.\n\nNot changed: tools/toolchain.py's docstring still names CI as an entry point. That file travels verbatim from sawlang (its own docstring says so), so it is not edited per repository. The \"never CI\" lines in CLAUDE.md, the Makefile and sos_runner (the esp32c3 smoke) stay true.\n\n## Acceptance\n\n- [ ] ci.yml triggers on workflow_dispatch only\n- [ ] no push to main triggers an Actions run after this merges\n","id":"SO-19","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":1,"sequence":27,"stage":"backlog","status":"open","title":"Disable the GitHub Actions test workflow; the gate moves to sawtracker","updated":"1790265633"},"id":"SO-19","project":"SO","request":"{\"acceptance\":[{\"checked\":false,\"text\":\"ci.yml triggers on workflow_dispatch only\"},{\"checked\":false,\"text\":\"no push to main triggers an Actions run after this merges\"}],\"action\":\"edit\",\"actor\":\"agent:claude-sawos\",\"id\":\"SO-19\",\"request_id\":\"pid-80336-1790265638-12000\"}","request_id":"pid-80336-1790265638-12000","sequence":28,"timestamp":"1790265638"}
---
