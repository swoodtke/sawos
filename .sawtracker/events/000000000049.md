---
{"action":"edit","actor":"agent:claude-sawos","after":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note"},{"checked_at":"","checked_by":"","text":"The Sep 7 bump's content lands unchanged"},{"checked_at":"","checked_by":"","text":"The slab + thread-preempt fixes land"},{"checked_at":"","checked_by":"","text":"The FINAL gate (server once SO-20 lands, else the reviewer) passes 382/382 on the patch tree, transcript-diffed; the developer's targeted cases are posted (23/23)"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[{"author":"agent:claude-sawos","body":"Submitted patch SO-21.p1 (revision 1): Pin sawlang at 454207de (0.11.1): Sep 7 bump + slab borrows -> &var T + thread-preempt ';' split","created":"1790266592","id":"c1"},{"author":"agent:claude-sawos","body":"Developer evidence (claude-sawos), under the Sep 24 targeted-tests ruling (chat m169, extended to sawos by the user). I ran NO full gate on the patch tree. The final gate is the server's once SO-20 lands, and until then the reviewer's. I stopped my queued full run and the v0.11.1 baseline part-way; the baseline's partial results are below.\n\n1. **Targeted, on the EXACT patch tree** (7bc6d3c = the posted diff on 22f876b), via the PINNED FETCH path (no SAWLANG_ROOT, no sawc on PATH; toolchain.py step 3 cold-cloned 454207de). The cases cover what the patch touches: `thread_preempt` (the edited test) and every slab-donation case. Those drive `Slab.[]` through BOTH its inline path and its donated-extent path (`extent_addr`), and `Slab.[]` is the accessor whose receiver changed. Cases: thread_preempt, slab_donate, slab_donate_shared, slab_donate_mapped, slab_donate_free_nodes, thread_donate, pipe_donate, process_donate. **23/23 pass**: riscv32 8/8, arm64 7/7 (slab_donate_free_nodes doesn't run there), riscv32-flat 8/8.\n2. **Full gate at head, run BEFORE the ruling** (same source minus the slab doc comment and the pin file; SAWLANG_ROOT = fresh clone at 454207de): **382/382 = 129 + 129 + 124**.\n3. **Transcript diff against the v0.11.1 baseline** (aa9e88f at v0.11.1; the part that completed before I stopped it): riscv32 all **129 rows identical**; arm64 rows **1-76 identical**. Only image sizes move: 137 riscv32 images total 2,261,736 -> 2,221,648 B (-40 KB), each between -1184 and +1720 B. The newer compiler's codegen accounts for that, and no row reads a size.\n\nAcceptance item 4 is reworded to match the ruling. Unchanged items keep their ticks.\n","created":"1790271776","id":"c2"}],"created":"1790266548","description":"## Scope\n\nPin sawlang at current main **`454207de`**, which reports **0.11.1**. **User ruling (Sep 24, in session with claude-sawos): \"pin to head as 0.11.1.\"** No tag is needed. `tools/toolchain.py` never reads tags: the fetch path uses `sha`, and `version` is checked only against a `sawc` on `$PATH`. The version is honest, since head reports 0.11.1. That a real v0.11.1 `sawc` on `$PATH` also satisfies it is D-b2's documented asymmetry for an unreleased compiler, and the pin's comment now says so. That answers claude-sos-reviewer's m158 point (1).\n\nTwo things land together, because GitHub never received the first:\n\n1. **The Sep 7 bump, 0.8.0 → 0.11.1** (`aa9e88f` on the old local main, NEVER PUSHED; chat m167). It is replayed unchanged onto origin/main: its only difference from aa9e88f is ci.yml + .sawtracker. It carries SL-27's `sizeof` derive in pipe.saw, design 245's `chars()` → `scalars()` sweep (`sosrt.ascii_byte` takes a `Scalar`), and the designs/todo.md SL-26/27/28 records. It was gated 382/382 at v0.11.1 on Sep 7.\n2. **What head costs beyond 0.11.1.** A full gate at head found two source breaks and nothing else:\n   - `kernel/core/slab.saw` Slab.[]: `(&var self, i: Int) unsafe borrows -> &var T` (was `&self ... borrows -> T`). SL-333 makes the arrow name the lent reference and its mode, and `-> &var T` requires `&var self`. The body only lends, so the accessor stays SHARED-ELIGIBLE per the spec's \"Receiver access is a second fact\": a read through `EVENTS[i]` still borrows the slab shared. A doc comment says so.\n   - `tests/thread-preempt/src/main.saw:157`: SL-347 refuses two statements on one line with nothing between them. The fix uses the tree's multi-line catch-guard shape (the user's Sep 22 style ruling: one line or fully broken).\n\nA parse check of all 205 tracked `.saw` files with head's parser: clean.\n\n## Not in scope\n\nSL-355's reactor_register ABI (0.12.0) is not at head. SO-18 keeps that (taskdump.saw's ENOSYS port, then the pin to the 0.12.0 cut) and stacks on this.\n\n## Evidence\n\n- Full gate at head (SAWLANG_ROOT = fresh HTTPS clone at 454207de; aa9e88f + the two fixes): **382/382 = 129 + 129 + 124**.\n- Pinned FETCH path (no SAWLANG_ROOT, no sawc on PATH): `toolchain.py` resolves `via: fetch` and cold-clones 454207de with its venv.\n- In progress: the final gate on the exact patch tree via the fetch path, diffed against a v0.11.1 baseline transcript of aa9e88f. Results post here.\n\n## Acceptance\n\n- [ ] `sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\n- [ ] The Sep 7 bump's content lands unchanged\n- [ ] The slab + thread-preempt fixes land\n- [ ] `make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\n","id":"SO-21","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":6,"sequence":49,"stage":"backlog","status":"open","title":"Pin sawlang at head 454207de (reports 0.11.1): land the unpushed Sep 7 bump + head's two source fixes","updated":"1790271776"},"before":{"acceptance":[{"checked_at":"","checked_by":"","text":"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note"},{"checked_at":"","checked_by":"","text":"The Sep 7 bump's content lands unchanged"},{"checked_at":"","checked_by":"","text":"The slab + thread-preempt fixes land"},{"checked_at":"","checked_by":"","text":"`make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","closed":"","comments":[{"author":"agent:claude-sawos","body":"Submitted patch SO-21.p1 (revision 1): Pin sawlang at 454207de (0.11.1): Sep 7 bump + slab borrows -> &var T + thread-preempt ';' split","created":"1790266592","id":"c1"},{"author":"agent:claude-sawos","body":"Developer evidence (claude-sawos), under the Sep 24 targeted-tests ruling (chat m169, extended to sawos by the user). I ran NO full gate on the patch tree. The final gate is the server's once SO-20 lands, and until then the reviewer's. I stopped my queued full run and the v0.11.1 baseline part-way; the baseline's partial results are below.\n\n1. **Targeted, on the EXACT patch tree** (7bc6d3c = the posted diff on 22f876b), via the PINNED FETCH path (no SAWLANG_ROOT, no sawc on PATH; toolchain.py step 3 cold-cloned 454207de). The cases cover what the patch touches: `thread_preempt` (the edited test) and every slab-donation case. Those drive `Slab.[]` through BOTH its inline path and its donated-extent path (`extent_addr`), and `Slab.[]` is the accessor whose receiver changed. Cases: thread_preempt, slab_donate, slab_donate_shared, slab_donate_mapped, slab_donate_free_nodes, thread_donate, pipe_donate, process_donate. **23/23 pass**: riscv32 8/8, arm64 7/7 (slab_donate_free_nodes doesn't run there), riscv32-flat 8/8.\n2. **Full gate at head, run BEFORE the ruling** (same source minus the slab doc comment and the pin file; SAWLANG_ROOT = fresh clone at 454207de): **382/382 = 129 + 129 + 124**.\n3. **Transcript diff against the v0.11.1 baseline** (aa9e88f at v0.11.1; the part that completed before I stopped it): riscv32 all **129 rows identical**; arm64 rows **1-76 identical**. Only image sizes move: 137 riscv32 images total 2,261,736 -> 2,221,648 B (-40 KB), each between -1184 and +1720 B. The newer compiler's codegen accounts for that, and no row reads a size.\n\nAcceptance item 4 is reworded to match the ruling. Unchanged items keep their ticks.\n","created":"1790271776","id":"c2"}],"created":"1790266548","description":"## Scope\n\nPin sawlang at current main **`454207de`**, which reports **0.11.1**. **User ruling (Sep 24, in session with claude-sawos): \"pin to head as 0.11.1.\"** No tag is needed. `tools/toolchain.py` never reads tags: the fetch path uses `sha`, and `version` is checked only against a `sawc` on `$PATH`. The version is honest, since head reports 0.11.1. That a real v0.11.1 `sawc` on `$PATH` also satisfies it is D-b2's documented asymmetry for an unreleased compiler, and the pin's comment now says so. That answers claude-sos-reviewer's m158 point (1).\n\nTwo things land together, because GitHub never received the first:\n\n1. **The Sep 7 bump, 0.8.0 → 0.11.1** (`aa9e88f` on the old local main, NEVER PUSHED; chat m167). It is replayed unchanged onto origin/main: its only difference from aa9e88f is ci.yml + .sawtracker. It carries SL-27's `sizeof` derive in pipe.saw, design 245's `chars()` → `scalars()` sweep (`sosrt.ascii_byte` takes a `Scalar`), and the designs/todo.md SL-26/27/28 records. It was gated 382/382 at v0.11.1 on Sep 7.\n2. **What head costs beyond 0.11.1.** A full gate at head found two source breaks and nothing else:\n   - `kernel/core/slab.saw` Slab.[]: `(&var self, i: Int) unsafe borrows -> &var T` (was `&self ... borrows -> T`). SL-333 makes the arrow name the lent reference and its mode, and `-> &var T` requires `&var self`. The body only lends, so the accessor stays SHARED-ELIGIBLE per the spec's \"Receiver access is a second fact\": a read through `EVENTS[i]` still borrows the slab shared. A doc comment says so.\n   - `tests/thread-preempt/src/main.saw:157`: SL-347 refuses two statements on one line with nothing between them. The fix uses the tree's multi-line catch-guard shape (the user's Sep 22 style ruling: one line or fully broken).\n\nA parse check of all 205 tracked `.saw` files with head's parser: clean.\n\n## Not in scope\n\nSL-355's reactor_register ABI (0.12.0) is not at head. SO-18 keeps that (taskdump.saw's ENOSYS port, then the pin to the 0.12.0 cut) and stacks on this.\n\n## Evidence\n\n- Full gate at head (SAWLANG_ROOT = fresh HTTPS clone at 454207de; aa9e88f + the two fixes): **382/382 = 129 + 129 + 124**.\n- Pinned FETCH path (no SAWLANG_ROOT, no sawc on PATH): `toolchain.py` resolves `via: fetch` and cold-clones 454207de with its venv.\n- In progress: the final gate on the exact patch tree via the fetch path, diffed against a v0.11.1 baseline transcript of aa9e88f. Results post here.\n\n## Acceptance\n\n- [ ] `sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\n- [ ] The Sep 7 bump's content lands unchanged\n- [ ] The slab + thread-preempt fixes land\n- [ ] `make sos-test` 382/382 on the patch tree via the pinned fetch path, and the transcript diff against the 0.11.1 baseline shows only rows the fixes explain\n","id":"SO-21","labels":["pin-bump"],"order":0,"parent":"","priority":"high","project":"SO","queue_order":0,"revision":5,"sequence":48,"stage":"backlog","status":"open","title":"Pin sawlang at head 454207de (reports 0.11.1): land the unpushed Sep 7 bump + head's two source fixes","updated":"1790271776"},"id":"SO-21","project":"SO","request":"{\"acceptance\":[{\"checked\":false,\"text\":\"`sawlang.pin` = 0.11.1 / 454207de, both lines together, with the untagged-sha note\"},{\"checked\":false,\"text\":\"The Sep 7 bump's content lands unchanged\"},{\"checked\":false,\"text\":\"The slab + thread-preempt fixes land\"},{\"checked\":false,\"text\":\"The FINAL gate (server once SO-20 lands, else the reviewer) passes 382/382 on the patch tree, transcript-diffed; the developer's targeted cases are posted (23/23)\"}],\"action\":\"edit\",\"actor\":\"agent:claude-sawos\",\"id\":\"SO-21\",\"request_id\":\"pid-6131-1790271776-14000\"}","request_id":"pid-6131-1790271776-14000","sequence":49,"timestamp":"1790271776"}
---
