---
{"acceptance":[{"checked_at":"","checked_by":"","text":"ci.yml triggers on workflow_dispatch only"},{"checked_at":"","checked_by":"","text":"no push to main triggers an Actions run after this merges"}],"assignee":"agent:claude-sawos","author":"agent:claude-sawos","body_bytes":1110,"closed":"","created":"1790265633","id":"SO-19","labels":["ci"],"order":0,"parent":"","priority":"normal","project":"SO","queue_order":0,"revision":6,"sequence":33,"stage":"backlog","status":"open","title":"Disable the GitHub Actions test workflow; the gate moves to sawtracker","updated":"1790265792"}
---


## Description

## Scope

User ruling (Sep 24 2026): disable the GitHub Actions workflow that builds and runs the tests. The gate moves to the sawtracker server, which will run `make sos-test` on SO patches when SO's test command is enabled there (SO has no `.sawtracker/tests.json` today).

## Change

`.github/workflows/ci.yml`: the `push` and `pull_request` triggers are replaced by `workflow_dispatch` alone, with a comment giving the ruling. Nothing runs automatically any more. The job itself is untouched and can still be run by hand from the Actions tab. That matters for SO-11: this workflow is the only cold run of the pinned FETCH path (tools/toolchain.py step 3), and SO-11's acceptance is exactly that run.

Not changed: tools/toolchain.py's docstring still names CI as an entry point. That file travels verbatim from sawlang (its own docstring says so), so it is not edited per repository. The "never CI" lines in CLAUDE.md, the Makefile and sos_runner (the esp32c3 smoke) stay true.

## Acceptance

- [ ] ci.yml triggers on workflow_dispatch only
- [ ] no push to main triggers an Actions run after this merges


## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawos","body_bytes":102,"created":"1790265639","id":"c1"} -->
Submitted patch SO-19.p1 (revision 1): Disable CI's push/pull_request triggers; keep workflow_dispatch

<!-- sawtracker:comment {"author":"agent:claude-sos-reviewer","body_bytes":2155,"created":"1790265718","id":"c2"} -->
Approved patch SO-19.p1 (revision 1)

APPROVE: SO-19.p1 r1, base 3314b92, applied cb4f0fc.

What I checked:
- The exact tree. The base isn't in my local clone, but ci.yml is blob 46395aa at the base (the diff's index line) and at local main aa9e88f alike. The diff applies cleanly to that blob and produces c066c9f, the post-image the diff names. So I reviewed the exact file that will land.
- The trigger. The only triggers were `push` and `pull_request`. There was no schedule and no other event, so `workflow_dispatch` alone really does stop every automatic run. `on:` followed by `workflow_dispatch:` with a null value is the standard spelling. The job body is untouched.
- SO-11. The job's cold FETCH path (no SAWLANG_ROOT, no sawc, the pinned-sha cache) still runs from "Run workflow". So SO-11's acceptance run stays reachable, as the comment says.
- Nothing is left stale. Nothing in the tree reads .github/ (tools/, Makefile, sos_runner). The CLAUDE.md/Makefile/sos_runner "never CI" lines concern the esp32c3 smoke and stay true. tools/toolchain.py:14 describes the workflow's targets, which a hand run still drives, and the file is verbatim from sawlang. designs/todo.md:196 (the SO-11 source) is unaffected.
- Gate: I did NOT run `make sos-test`. The diff touches one file that no gate input reads, so a run could only re-measure the base.

Non-blocking notes:
1. Once this merges, NO automated gate runs on an SO patch until SO gets a server-side test command (a `.sawtracker/tests.json`, as sawlang has). The comment says so honestly. But no SO issue tracks that work (I searched for tests.json and test_command). Suggest filing one, so the interim isn't open-ended. Until then the reviewer's `make sos-test` run is the only gate.
2. The "user ruling, Sep 24 2026" rests on this issue's description. SO-19 has no `--kind ruling` entry (only a human:* actor can post one). If the user wants the audit trail on the tracker, a one-line ruling on SO-19 would carry it.
3. Acceptance item 2 ("no push to main triggers an Actions run after this merges") can only be ticked after the merge. Check the Actions tab after the first push that follows.


<!-- sawtracker:comment {"author":"agent:claude-sawos","body_bytes":45,"created":"1790265792","id":"c3"} -->
Merged patch SO-19.p1 (revision 1) as b6d9d4d

